// Code generated by openapi2crd. DO NOT EDIT.

// Package v1alpha1 contains the Cluster-scoped IPv4SharedNetwork managed resource.
// API group: ipv4sharednetwork.infobloxnios.crossplane.io
package v1alpha1

import (
	xpv1 "github.com/crossplane/crossplane-runtime/v2/apis/common/v1"
	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
)

// IPv4SharedNetworkDhcpOption is a DHCP option associated with an IPv4SharedNetwork, mirroring the SDK's Dhcpoption struct.
type IPv4SharedNetworkDhcpOption struct {
	// Name of the DHCP option.
	Name *string `json:"name,omitempty"`
	// Code of the DHCP option.
	Num *uint32 `json:"num,omitempty"`
	// Name of the space this DHCP option is associated with.
	VendorClass *string `json:"vendorClass,omitempty"`
	// Value of the DHCP option.
	Value *string `json:"value,omitempty"`
	// Only applies to special options displayed separately from other options with a use flag (e.g. routers, domain-name-servers, dhcp-lease-time).
	UseOption *bool `json:"useOption,omitempty"`
}

// IPv4SharedNetworkParameters are the configurable fields of a IPv4SharedNetwork.
type IPv4SharedNetworkParameters struct {
	// Display name of the shared network.
	// +kubebuilder:validation:Required
	Name *string `json:"name"`
	// CIDRs of the member networks combined into this shared network. Each entry must match an existing Network resource's network (CIDR) field — a cidr-match cross-resource reference, not a _ref or name lookup. Not yet wired with the Ref/Selector reference pattern: the Network managed resource is not merged to main in this provider yet; wiring is deferred to a follow-up ticket to avoid an angryjet resolver import of a nonexistent package.
	// +kubebuilder:validation:Required
	Networks []string `json:"networks"`
	// Network view the shared network belongs to. Present in both the Create and Update ObjectManager wrapper signatures, but live WAPI _schema probing found supports=rws (no u) for this field — the WAPI schema rejects changing it after creation even though the wrapper accepts the parameter on update calls.
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="networkView is immutable after creation"
	NetworkView *string `json:"networkView,omitempty"`
	// Comment for the shared network.
	Comment *string `json:"comment,omitempty"`
	// Extensible attributes (arbitrary key/value metadata defined in Grid Manager). The WAPI wire format wraps each value as {"value": ...}; this map is the simplified string-valued CRD representation (the controller translates to/from the SDK's EA map[string]interface{} type).
	// +optional
	ExtAttrs map[string]string `json:"extAttrs"`
	// Whether the shared network is disabled. When false, the shared network is enabled.
	Disable *bool `json:"disable,omitempty"`
	// Use flag for the options field.
	UseOptions *bool `json:"useOptions,omitempty"`
	// DHCP options associated with the shared network.
	// +optional
	Options []*IPv4SharedNetworkDhcpOption `json:"options"`
}

// IPv4SharedNetworkObservation holds the observed state of a IPv4SharedNetwork.
// It mirrors every IPv4SharedNetworkParameters field plus response-only fields
// (full-mirror AtProvider) so users can compare forProvider to
// atProvider before switching to full management
// (managementPolicies: ["Observe"]).
type IPv4SharedNetworkObservation struct {
	// Server-assigned external identifier for this resource, set by Observe()
	// from the resource's external name. Used by import testing to confirm
	// the controller re-discovers the resource after a restart.
	// +optional
	ID string `json:"id,omitempty"` // atProvider
	// Display name of the shared network.
	Name *string `json:"name,omitempty"` // atProvider
	// CIDRs of the member networks combined into this shared network. Each entry must match an existing Network resource's network (CIDR) field — a cidr-match cross-resource reference, not a _ref or name lookup. Not yet wired with the Ref/Selector reference pattern: the Network managed resource is not merged to main in this provider yet; wiring is deferred to a follow-up ticket to avoid an angryjet resolver import of a nonexistent package.
	// +optional
	Networks []string `json:"networks"` // atProvider
	// Network view the shared network belongs to. Present in both the Create and Update ObjectManager wrapper signatures, but live WAPI _schema probing found supports=rws (no u) for this field — the WAPI schema rejects changing it after creation even though the wrapper accepts the parameter on update calls.
	NetworkView *string `json:"networkView,omitempty"` // atProvider
	// Comment for the shared network.
	Comment *string `json:"comment,omitempty"` // atProvider
	// Extensible attributes (arbitrary key/value metadata defined in Grid Manager). The WAPI wire format wraps each value as {"value": ...}; this map is the simplified string-valued CRD representation (the controller translates to/from the SDK's EA map[string]interface{} type).
	// +optional
	ExtAttrs map[string]string `json:"extAttrs"` // atProvider
	// Whether the shared network is disabled. When false, the shared network is enabled.
	Disable *bool `json:"disable,omitempty"` // atProvider
	// Use flag for the options field.
	UseOptions *bool `json:"useOptions,omitempty"` // atProvider
	// DHCP options associated with the shared network.
	// +optional
	Options []*IPv4SharedNetworkDhcpOption `json:"options"` // atProvider
	// Server-assigned opaque object reference (WAPI `_ref`). Mirrors the crossplane.io/external-name annotation for observability and uptest import verification.
	Ref *string `json:"ref,omitempty"` // atProvider
	// Authority for the shared network.
	Authority *bool `json:"authority,omitempty"` // atProvider
	// DNS update Time-to-live (TTL) value, in seconds. Zero means the update is not cached.
	DdnsTTL *uint32 `json:"ddnsTtl,omitempty"` // atProvider
	// Whether the DHCP server sends DDNS updates for this shared network.
	EnableDdns *bool `json:"enableDdns,omitempty"` // atProvider
	// DHCP utilization percentage across member networks, multiplied by 1000.
	DhcpUtilization *uint32 `json:"dhcpUtilization,omitempty"` // atProvider
	// Utilization level descriptor (e.g. "NORMAL", "WARNING").
	DhcpUtilizationStatus *string `json:"dhcpUtilizationStatus,omitempty"` // atProvider
	// Total number of DHCP leases issued for the shared network.
	DynamicHosts *uint32 `json:"dynamicHosts,omitempty"` // atProvider
}

// IPv4SharedNetworkSpec defines the desired state of IPv4SharedNetwork.
type IPv4SharedNetworkSpec struct {
	xpv1.ResourceSpec `json:",inline"`

	// ForProvider holds the user-supplied parameters for this IPv4SharedNetwork.
	// +kubebuilder:validation:Required
	ForProvider IPv4SharedNetworkParameters `json:"forProvider"`
}

// IPv4SharedNetworkStatus defines the observed state of IPv4SharedNetwork.
type IPv4SharedNetworkStatus struct {
	xpv1.ResourceStatus `json:",inline"`

	// AtProvider holds the observed values from the Infoblox NIOS WAPI.
	// +optional
	AtProvider IPv4SharedNetworkObservation `json:"atProvider,omitempty"`
}

// +kubebuilder:object:root=true
// +kubebuilder:storageversion
// +kubebuilder:subresource:status
// +kubebuilder:resource:scope=Cluster,categories={crossplane,managed,infobloxnios}
// +kubebuilder:printcolumn:name="READY",type="string",JSONPath=".status.conditions[?(@.type=='Ready')].status"
// +kubebuilder:printcolumn:name="SYNCED",type="string",JSONPath=".status.conditions[?(@.type=='Synced')].status"
// +kubebuilder:printcolumn:name="EXTERNAL-NAME",type="string",JSONPath=".metadata.annotations.crossplane\\.io/external-name"
// +kubebuilder:printcolumn:name="AGE",type="date",JSONPath=".metadata.creationTimestamp"

// IPv4SharedNetwork is the Cluster-scoped managed resource representing an Infoblox NIOS IPv4SharedNetwork.
type IPv4SharedNetwork struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`

	Spec   IPv4SharedNetworkSpec   `json:"spec"`
	Status IPv4SharedNetworkStatus `json:"status,omitempty"`
}

// +kubebuilder:object:root=true

// IPv4SharedNetworkList contains a list of IPv4SharedNetwork.
type IPv4SharedNetworkList struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []IPv4SharedNetwork `json:"items"`
}
