// Code generated by openapi2crd. DO NOT EDIT.

// Package extensibleattributedef contains a standalone reference copy of the Parameters
// and Observation types for the ExtensibleAttributeDef managed resource. The
// cluster-scoped and namespaced variants (apis/cluster/extensibleattributedef/,
// apis/namespaced/extensibleattributedef/) generate self-contained copies of these same
// types rather than importing this package — see the openapi2crd/pkg/
// generator package doc comment for the rationale.
package extensibleattributedef

// EADefListValue holds one enumerated value entry for an ExtensibleAttributeDef with type=ENUM.
type EADefListValue struct {
	// The enumerated value.
	// +kubebuilder:validation:Required
	Value string `json:"value"`
}

// ExtensibleAttributeDefParameters are the configurable fields of a ExtensibleAttributeDef.
type ExtensibleAttributeDefParameters struct {
	// Name of the Extensible Attribute Definition. Mutable — renaming changes the record's _ref.
	// +kubebuilder:validation:Required
	Name string `json:"name"`
	// Data type of the extensible attribute's value. Immutable — WAPI rejects a change with "Type of extensible attribute definition cannot be modified".
	// +kubebuilder:validation:Required
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="type is immutable after creation"
	// +kubebuilder:validation:Enum=STRING;INTEGER;ENUM;DATE;EMAIL;URL
	Type string `json:"type"`
	// Comment for the Extensible Attribute Definition; maximum 256 characters. Mutable.
	Comment *string `json:"comment,omitempty"`
	// Default value used to pre-populate the attribute value in the GUI. For email, URL, and string types, a string with a maximum of 256 characters. For an integer, an integer from -2147483648 through 2147483647. For a date, the number of seconds elapsed since January 1st, 1970 UTC. Mutable.
	DefaultValue *string `json:"defaultValue,omitempty"`
	// Minimum allowed value of the extensible attribute. Applicable if type=INTEGER. Immutable — WAPI rejects a change with "Minimum value cannot be modified".
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="min is immutable after creation"
	Min *uint32 `json:"min,omitempty"`
	// Maximum allowed value of the extensible attribute. Applicable if type=INTEGER. Immutable — same family as min, rejected by WAPI on update.
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="max is immutable after creation"
	Max *uint32 `json:"max,omitempty"`
	// Extensible attribute flags. Possible letters, most-significant first: (A)udited, (C)loud API, Cloud (G)master, (I)nheritable, (L)isted, (M)andatory value, MGM (P)rivate, (R)ead Only, (S)ort enum values, Multiple (V)alues. If there are two or more flags they must be listed in the order shown above (e.g. "CR" is valid, "RC" is not). Mutable.
	Flags *string `json:"flags,omitempty"`
	// Enumerated valid values. Applicable if type=ENUM. Mutable.
	// +optional
	ListValues []EADefListValue `json:"listValues"`
	// WAPI object types this attribute is allowed to associate with (empty means all types). Mutable.
	// +optional
	AllowedObjectTypes []string `json:"allowedObjectTypes"`
	// Action taken on descendant objects carrying this attribute when it is Inheritable. Write-only — the WAPI field schema marks it not readable, so it is never mirrored into the observed state; a controller must never request it in _return_fields.
	DescendantsAction *string `json:"descendantsAction,omitempty"`
}

// ExtensibleAttributeDefObservation holds the observed state of a ExtensibleAttributeDef.
// It mirrors every ExtensibleAttributeDefParameters field plus response-only fields
// (full-mirror AtProvider) so users can compare forProvider to
// atProvider before switching to full management
// (managementPolicies: ["Observe"]).
type ExtensibleAttributeDefObservation struct {
	// Server-assigned external identifier for this resource, set by Observe()
	// from the resource's external name. Used by import testing to confirm
	// the controller re-discovers the resource after a restart.
	// +optional
	ID string `json:"id,omitempty"` // atProvider
	// Name of the Extensible Attribute Definition. Mutable — renaming changes the record's _ref.
	Name *string `json:"name,omitempty"` // atProvider
	// Data type of the extensible attribute's value. Immutable — WAPI rejects a change with "Type of extensible attribute definition cannot be modified".
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="type is immutable after creation"
	Type *string `json:"type,omitempty"` // atProvider
	// Comment for the Extensible Attribute Definition; maximum 256 characters. Mutable.
	Comment *string `json:"comment,omitempty"` // atProvider
	// Default value used to pre-populate the attribute value in the GUI. For email, URL, and string types, a string with a maximum of 256 characters. For an integer, an integer from -2147483648 through 2147483647. For a date, the number of seconds elapsed since January 1st, 1970 UTC. Mutable.
	DefaultValue *string `json:"defaultValue,omitempty"` // atProvider
	// Minimum allowed value of the extensible attribute. Applicable if type=INTEGER. Immutable — WAPI rejects a change with "Minimum value cannot be modified".
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="min is immutable after creation"
	Min *uint32 `json:"min,omitempty"` // atProvider
	// Maximum allowed value of the extensible attribute. Applicable if type=INTEGER. Immutable — same family as min, rejected by WAPI on update.
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="max is immutable after creation"
	Max *uint32 `json:"max,omitempty"` // atProvider
	// Extensible attribute flags. Possible letters, most-significant first: (A)udited, (C)loud API, Cloud (G)master, (I)nheritable, (L)isted, (M)andatory value, MGM (P)rivate, (R)ead Only, (S)ort enum values, Multiple (V)alues. If there are two or more flags they must be listed in the order shown above (e.g. "CR" is valid, "RC" is not). Mutable.
	Flags *string `json:"flags,omitempty"` // atProvider
	// Enumerated valid values. Applicable if type=ENUM. Mutable.
	// +optional
	ListValues []EADefListValue `json:"listValues"` // atProvider
	// WAPI object types this attribute is allowed to associate with (empty means all types). Mutable.
	// +optional
	AllowedObjectTypes []string `json:"allowedObjectTypes"` // atProvider
	// Server-assigned opaque object reference (WAPI `_ref`). Mirrors the crossplane.io/external-name annotation for observability and uptest import verification.
	Ref *string `json:"ref,omitempty"` // atProvider
	// Namespace for the Extensible Attribute Definition. Read-only.
	Namespace *string `json:"namespace,omitempty"` // atProvider
}
