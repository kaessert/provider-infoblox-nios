// Code generated by openapi2crd. DO NOT EDIT.

// Package zoneforward contains a standalone reference copy of the Parameters
// and Observation types for the ZoneForward managed resource. The
// cluster-scoped and namespaced variants (apis/cluster/zoneforward/,
// apis/namespaced/zoneforward/) generate self-contained copies of these same
// types rather than importing this package — see the openapi2crd/pkg/
// generator package doc comment for the rationale.
package zoneforward

// NameServer identifies an external DNS server used as a forwarding target.
type NameServer struct {
	// A resolvable domain name for the external DNS server.
	Name *string `json:"name,omitempty"`
	// The IPv4 or IPv6 address of the server.
	Address *string `json:"address,omitempty"`
}

// ForwardingServer overrides the forwarders used by a specific Grid member for this zone.
type ForwardingServer struct {
	// The name of this Grid member in FQDN format.
	Name *string `json:"name,omitempty"`
	// Determines if this Grid member sends queries to forwardTo only, and not to other internal or Internet root servers.
	ForwardersOnly *bool `json:"forwardersOnly,omitempty"`
	// The remote name servers to which this Grid member forwards queries for this zone, overriding the zone-level forwardTo list.
	// +optional
	ForwardTo []NameServer `json:"forwardTo"`
	// Use flag for forwardTo — when false this Grid member uses the zone-level forwarders instead of the override.
	UseOverrideForwarders *bool `json:"useOverrideForwarders,omitempty"`
}

// ZoneForwardParameters are the configurable fields of a ZoneForward.
type ZoneForwardParameters struct {
	// The name of this DNS zone in FQDN format. Fixed at creation — confirmed absent from the UpdateZoneForward SDK method signature.
	// +kubebuilder:validation:Required
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="fqdn is immutable after creation"
	Fqdn *string `json:"fqdn"`
	// The remote name servers to which the Infoblox appliance forwards queries for this zone.
	// +kubebuilder:validation:Required
	ForwardTo []NameServer `json:"forwardTo"`
	// The name of the DNS view in which the zone resides, e.g. "external". Fixed at creation — confirmed absent from the UpdateZoneForward SDK method signature; the WAPI additionally rejects a view change at the data level with "Cannot move zones between views".
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="view is immutable after creation"
	View *string `json:"view,omitempty"`
	// Determines the format of this zone (e.g. FORWARD, IPV4, IPV6). Fixed at creation — confirmed absent from the UpdateZoneForward SDK method signature.
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="zoneFormat is immutable after creation"
	ZoneFormat *string `json:"zoneFormat,omitempty"`
	// Comment for the zone; maximum 256 characters.
	Comment *string `json:"comment,omitempty"`
	// Determines whether the zone is disabled. When false, the zone is enabled.
	Disable *bool `json:"disable,omitempty"`
	// Determines if the appliance sends queries to the servers in forwardTo only, and not to other internal or Internet root servers.
	ForwardersOnly *bool `json:"forwardersOnly,omitempty"`
	// A forwarding stub server name server group name.
	NsGroup *string `json:"nsGroup,omitempty"`
	// A forward stub server name server group name.
	ExternalNsGroup *string `json:"externalNsGroup,omitempty"`
	// Per-Grid-member forwarding server overrides for this zone.
	// +optional
	ForwardingServers []ForwardingServer `json:"forwardingServers"`
	// Extensible attributes (arbitrary key/value metadata defined in Grid Manager). The WAPI wire format wraps each value as {"value": ...}; this map is the simplified string-valued CRD representation (the controller translates to/from the SDK's EA map[string]interface{} type).
	// +optional
	// +kubebuilder:validation:XValidation:rule="self.all(k, k != 'Crossplane Internal ID')",message="the 'Crossplane Internal ID' extensible attribute is reserved for the provider's identity stamp and cannot be set in spec.forProvider.extAttrs"
	ExtAttrs map[string]string `json:"extAttrs"`
}

// ZoneForwardObservation holds the observed state of a ZoneForward.
// It mirrors every ZoneForwardParameters field plus response-only fields
// (full-mirror AtProvider) so users can compare forProvider to
// atProvider before switching to full management
// (managementPolicies: ["Observe"]).
type ZoneForwardObservation struct {
	// Server-assigned external identifier for this resource, set by Observe()
	// from the resource's external name. Used by import testing to confirm
	// the controller re-discovers the resource after a restart.
	// +optional
	ID string `json:"id,omitempty"` // atProvider
	// The name of this DNS zone in FQDN format. Fixed at creation — confirmed absent from the UpdateZoneForward SDK method signature.
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="fqdn is immutable after creation"
	Fqdn *string `json:"fqdn,omitempty"` // atProvider
	// The remote name servers to which the Infoblox appliance forwards queries for this zone.
	// +optional
	ForwardTo []NameServer `json:"forwardTo"` // atProvider
	// The name of the DNS view in which the zone resides, e.g. "external". Fixed at creation — confirmed absent from the UpdateZoneForward SDK method signature; the WAPI additionally rejects a view change at the data level with "Cannot move zones between views".
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="view is immutable after creation"
	View *string `json:"view,omitempty"` // atProvider
	// Determines the format of this zone (e.g. FORWARD, IPV4, IPV6). Fixed at creation — confirmed absent from the UpdateZoneForward SDK method signature.
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="zoneFormat is immutable after creation"
	ZoneFormat *string `json:"zoneFormat,omitempty"` // atProvider
	// Comment for the zone; maximum 256 characters.
	Comment *string `json:"comment,omitempty"` // atProvider
	// Determines whether the zone is disabled. When false, the zone is enabled.
	Disable *bool `json:"disable,omitempty"` // atProvider
	// Determines if the appliance sends queries to the servers in forwardTo only, and not to other internal or Internet root servers.
	ForwardersOnly *bool `json:"forwardersOnly,omitempty"` // atProvider
	// A forwarding stub server name server group name.
	NsGroup *string `json:"nsGroup,omitempty"` // atProvider
	// A forward stub server name server group name.
	ExternalNsGroup *string `json:"externalNsGroup,omitempty"` // atProvider
	// Per-Grid-member forwarding server overrides for this zone.
	// +optional
	ForwardingServers []ForwardingServer `json:"forwardingServers"` // atProvider
	// Extensible attributes (arbitrary key/value metadata defined in Grid Manager). The WAPI wire format wraps each value as {"value": ...}; this map is the simplified string-valued CRD representation (the controller translates to/from the SDK's EA map[string]interface{} type).
	// +optional
	ExtAttrs map[string]string `json:"extAttrs"` // atProvider
	// Server-assigned opaque object reference (WAPI `_ref`). Mirrors the crossplane.io/external-name annotation for observability and uptest import verification.
	Ref *string `json:"ref,omitempty"` // atProvider
}
