// Code generated by openapi2crd. DO NOT EDIT.

// Package v1alpha1 contains the Namespaced-scoped HostRecord managed resource.
// API group: hostrecord.infobloxnios.m.crossplane.io
package v1alpha1

import (
	xpv1 "github.com/crossplane/crossplane-runtime/v2/apis/common/v1"
	xpv2 "github.com/crossplane/crossplane-runtime/v2/apis/common/v2"
	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
)

// HostRecordIpv4Addr holds one IPv4 address entry for a HostRecord.
type HostRecordIpv4Addr struct {
	// IPv4 address of the host (static or CIDR-allocated).
	// +kubebuilder:validation:Required
	Ipv4Addr string `json:"ipv4Addr"`
	// MAC address for DHCP association.
	MAC *string `json:"mac,omitempty"`
	// Whether to configure the address for DHCP.
	ConfigureForDHCP *bool `json:"configureForDhcp,omitempty"`
	// Next server address for PXE boot.
	NextServer *string `json:"nextServer,omitempty"`
}

// HostRecordIpv6Addr holds one IPv6 address entry for a HostRecord.
type HostRecordIpv6Addr struct {
	// IPv6 address of the host.
	// +kubebuilder:validation:Required
	Ipv6Addr string `json:"ipv6Addr"`
	// DHCP unique identifier for IPv6 DHCP association.
	Duid *string `json:"duid,omitempty"`
	// Whether to configure the address for DHCP.
	ConfigureForDHCP *bool `json:"configureForDhcp,omitempty"`
}

// HostRecordParameters are the configurable fields of a HostRecord.
type HostRecordParameters struct {
	// Host name in FQDN format. Renaming changes the record's _ref.
	// +kubebuilder:validation:Required
	Name *string `json:"name"`
	// List of IPv4 addresses for the host. At least one entry is required on create.
	// +kubebuilder:validation:Required
	Ipv4Addrs []HostRecordIpv4Addr `json:"ipv4Addrs"`
	// List of IPv6 addresses for the host.
	// +optional
	Ipv6Addrs []HostRecordIpv6Addr `json:"ipv6Addrs"`
	// Network view the host record resides in. Immutable after creation (live-verified: supports=rws, no u — corrects Phase 1 inventory).
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="networkView is immutable after creation"
	// +crossplane:generate:reference:type=github.com/crossplane-contrib/provider-infoblox-nios/apis/cluster/networkview/v1alpha1.NetworkView
	NetworkView *string `json:"networkView,omitempty"`
	// +optional
	NetworkViewRef *xpv1.NamespacedReference `json:"networkViewRef,omitempty"`
	// +optional
	NetworkViewSelector *xpv1.NamespacedSelector `json:"networkViewSelector,omitempty"`
	// DNS view in which the record resides. Mutable — changing it moves the record between DNS views and changes the _ref.
	View *string `json:"view,omitempty"`
	// List of alias FQDNs for the host.
	// +optional
	Aliases []string `json:"aliases"`
	// Whether the host has DNS (parent zone) information. When false, no DNS records are created.
	ConfigureForDNS *bool `json:"configureForDns,omitempty"`
	// Comment for the record; maximum 256 characters.
	Comment *string `json:"comment,omitempty"`
	// Whether the record is disabled.
	Disable *bool `json:"disable,omitempty"`
	// Time-to-live in seconds. Zero means the record is not cached. Must be non-negative (0-2147483647); to inherit the zone/grid default, set useTtl to false rather than passing a negative sentinel value.
	// +kubebuilder:validation:Minimum=0
	// +kubebuilder:validation:Maximum=2147483647
	TTL *uint32 `json:"ttl,omitempty"`
	// Use flag for ttl — when false the zone/grid default TTL applies.
	UseTTL *bool `json:"useTtl,omitempty"`
	// Extensible attributes (arbitrary key/value metadata defined in Grid Manager). The WAPI wire format wraps each value as {"value": ...}; this map is the simplified string-valued CRD representation (the controller translates to/from the SDK's EA map[string]interface{} type).
	// +optional
	ExtAttrs map[string]string `json:"extAttrs"`
	// CIDR of the IPv4 network from which to allocate the next available IP address (WAPI func:nextavailableip). Create-time-only — ignored on Update. Mutually exclusive with static ipv4Addrs entries that specify an address.
	Ipv4Cidr *string `json:"ipv4Cidr,omitempty"`
	// CIDR of the IPv6 network from which to allocate the next available IP address. Create-time-only — ignored on Update. Mutually exclusive with static ipv6Addrs entries that specify an address.
	Ipv6Cidr *string `json:"ipv6Cidr,omitempty"`
	// Extensible attribute key/value filter for EA-based IP allocation via AllocateNextAvailableIp. Mutually exclusive with ipv4Cidr/ipv6Cidr. When set, ipAddressType is required.
	// +optional
	FilterParams map[string]string `json:"filterParams"`
	// IP address type to allocate when using filterParams: "IPV4", "IPV6", or "Both". Required when filterParams is set; ignored otherwise.
	// +kubebuilder:validation:Enum=IPV4;IPV6;Both
	IpAddressType *string `json:"ipAddressType,omitempty"`
}

// HostRecordObservation holds the observed state of a HostRecord.
// It mirrors every HostRecordParameters field plus response-only fields
// (full-mirror AtProvider) so users can compare forProvider to
// atProvider before switching to full management
// (managementPolicies: ["Observe"]).
type HostRecordObservation struct {
	// Server-assigned external identifier for this resource, set by Observe()
	// from the resource's external name. Used by import testing to confirm
	// the controller re-discovers the resource after a restart.
	// +optional
	ID string `json:"id,omitempty"` // atProvider
	// Host name in FQDN format. Renaming changes the record's _ref.
	Name *string `json:"name,omitempty"` // atProvider
	// List of IPv4 addresses for the host. At least one entry is required on create.
	// +optional
	Ipv4Addrs []HostRecordIpv4Addr `json:"ipv4Addrs"` // atProvider
	// List of IPv6 addresses for the host.
	// +optional
	Ipv6Addrs []HostRecordIpv6Addr `json:"ipv6Addrs"` // atProvider
	// Network view the host record resides in. Immutable after creation (live-verified: supports=rws, no u — corrects Phase 1 inventory).
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="networkView is immutable after creation"
	NetworkView *string `json:"networkView,omitempty"` // atProvider
	// DNS view in which the record resides. Mutable — changing it moves the record between DNS views and changes the _ref.
	View *string `json:"view,omitempty"` // atProvider
	// List of alias FQDNs for the host.
	// +optional
	Aliases []string `json:"aliases"` // atProvider
	// Whether the host has DNS (parent zone) information. When false, no DNS records are created.
	ConfigureForDNS *bool `json:"configureForDns,omitempty"` // atProvider
	// Comment for the record; maximum 256 characters.
	Comment *string `json:"comment,omitempty"` // atProvider
	// Whether the record is disabled.
	Disable *bool `json:"disable,omitempty"` // atProvider
	// Time-to-live in seconds. Zero means the record is not cached. Must be non-negative (0-2147483647); to inherit the zone/grid default, set useTtl to false rather than passing a negative sentinel value.
	TTL *uint32 `json:"ttl,omitempty"` // atProvider
	// Use flag for ttl — when false the zone/grid default TTL applies.
	UseTTL *bool `json:"useTtl,omitempty"` // atProvider
	// Extensible attributes (arbitrary key/value metadata defined in Grid Manager). The WAPI wire format wraps each value as {"value": ...}; this map is the simplified string-valued CRD representation (the controller translates to/from the SDK's EA map[string]interface{} type).
	// +optional
	ExtAttrs map[string]string `json:"extAttrs"` // atProvider
	// CIDR of the IPv4 network from which to allocate the next available IP address (WAPI func:nextavailableip). Create-time-only — ignored on Update. Mutually exclusive with static ipv4Addrs entries that specify an address.
	Ipv4Cidr *string `json:"ipv4Cidr,omitempty"` // atProvider
	// CIDR of the IPv6 network from which to allocate the next available IP address. Create-time-only — ignored on Update. Mutually exclusive with static ipv6Addrs entries that specify an address.
	Ipv6Cidr *string `json:"ipv6Cidr,omitempty"` // atProvider
	// Extensible attribute key/value filter for EA-based IP allocation via AllocateNextAvailableIp. Mutually exclusive with ipv4Cidr/ipv6Cidr. When set, ipAddressType is required.
	// +optional
	FilterParams map[string]string `json:"filterParams"` // atProvider
	// IP address type to allocate when using filterParams: "IPV4", "IPV6", or "Both". Required when filterParams is set; ignored otherwise.
	IpAddressType *string `json:"ipAddressType,omitempty"` // atProvider
	// Server-assigned opaque object reference (WAPI `_ref`). Mirrors the crossplane.io/external-name annotation for observability and uptest import verification.
	Ref *string `json:"ref,omitempty"` // atProvider
	// Zone in which the record resides. Derived from name/view by WAPI — not a CreateHostRecord parameter.
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="zone is immutable after creation"
	Zone *string `json:"zone,omitempty"` // atProvider
	// Host name in punycode format (derived from name).
	DNSName *string `json:"dnsName,omitempty"` // atProvider
	// Alias FQDNs in punycode format.
	// +optional
	DNSAliases []string `json:"dnsAliases"` // atProvider
}

// HostRecordSpec defines the desired state of HostRecord.
type HostRecordSpec struct {
	xpv2.ManagedResourceSpec `json:",inline"`

	// ForProvider holds the user-supplied parameters for this HostRecord.
	// +kubebuilder:validation:Required
	ForProvider HostRecordParameters `json:"forProvider"`
}

// HostRecordStatus defines the observed state of HostRecord.
type HostRecordStatus struct {
	xpv1.ResourceStatus `json:",inline"`

	// AtProvider holds the observed values from the Infoblox NIOS WAPI.
	// +optional
	AtProvider HostRecordObservation `json:"atProvider,omitempty"`
}

// +kubebuilder:object:root=true
// +kubebuilder:storageversion
// +kubebuilder:subresource:status
// +kubebuilder:resource:scope=Namespaced,categories={crossplane,managed,infobloxnios}
// +kubebuilder:printcolumn:name="READY",type="string",JSONPath=".status.conditions[?(@.type=='Ready')].status"
// +kubebuilder:printcolumn:name="SYNCED",type="string",JSONPath=".status.conditions[?(@.type=='Synced')].status"
// +kubebuilder:printcolumn:name="EXTERNAL-NAME",type="string",JSONPath=".metadata.annotations.crossplane\\.io/external-name"
// +kubebuilder:printcolumn:name="AGE",type="date",JSONPath=".metadata.creationTimestamp"

// HostRecord is the Namespaced-scoped managed resource representing an Infoblox NIOS HostRecord.
type HostRecord struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`

	Spec   HostRecordSpec   `json:"spec"`
	Status HostRecordStatus `json:"status,omitempty"`
}

// +kubebuilder:object:root=true

// HostRecordList contains a list of HostRecord.
type HostRecordList struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []HostRecord `json:"items"`
}
