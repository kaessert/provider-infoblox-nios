// Code generated by openapi2crd. DO NOT EDIT.

// Package v1alpha1 contains the Namespaced-scoped RangeTemplate managed resource.
// API group: rangetemplate.infobloxnios.m.crossplane.io
package v1alpha1

import (
	xpv2 "github.com/crossplane/crossplane/apis/v2/core/v2"
	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"

	"github.com/crossplane-contrib/provider-infoblox-nios/apis/common/driftdetection"
)

// RangeTemplateOption carries one DHCP option to apply to ranges created from a RangeTemplate (mirrors the SDK's Dhcpoption struct).
type RangeTemplateOption struct {
	// Name of the DHCP option.
	Name *string `json:"name,omitempty"`
	// The code of the DHCP option.
	Num *uint32 `json:"num,omitempty"`
	// The name of the space this DHCP option is associated to.
	VendorClass *string `json:"vendorClass,omitempty"`
	// Value of the DHCP option.
	Value *string `json:"value,omitempty"`
	// Only applies to special options that are displayed separately from other options and have a use flag (routers, router-templates, domain-name-servers, domain-name, broadcast-address, broadcast-address-offset, dhcp-lease-time, dhcp6.name-servers).
	UseOption *bool `json:"useOption,omitempty"`
}

// RangeTemplateMember identifies the Grid member that will serve ranges created from a RangeTemplate (mirrors the SDK's Dhcpmember struct).
type RangeTemplateMember struct {
	// The IPv4 Address of the Grid Member.
	Ipv4Addr *string `json:"ipv4addr,omitempty"`
	// The IPv6 Address of the Grid Member.
	Ipv6Addr *string `json:"ipv6addr,omitempty"`
	// The Grid member name.
	Name *string `json:"name,omitempty"`
}

// RangeTemplateParameters are the configurable fields of a RangeTemplate.
type RangeTemplateParameters struct {
	// Name of the range template object.
	// +kubebuilder:validation:Required
	Name *string `json:"name"`
	// Number of addresses the range should contain when instantiated.
	// +kubebuilder:validation:Required
	NumberOfAddresses *uint32 `json:"numberOfAddresses"`
	// Start address offset within the parent network when instantiated.
	// +kubebuilder:validation:Required
	Offset *uint32 `json:"offset"`
	// Comment for the range template.
	Comment *string `json:"comment,omitempty"`
	// Extensible attributes (arbitrary key/value metadata defined in Grid Manager). The WAPI wire format wraps each value as {"value": ...}; this map is the simplified string-valued CRD representation (the controller translates to/from the SDK's EA map[string]interface{} type).
	// +optional
	// +kubebuilder:validation:XValidation:rule="self.all(k, k != 'Crossplane Internal ID')",message="the 'Crossplane Internal ID' extensible attribute is reserved for the provider's identity stamp and cannot be set in spec.forProvider.extAttrs"
	ExtAttrs map[string]string `json:"extAttrs"`
	// DHCP options to apply to ranges created from this template.
	// +optional
	Options []*RangeTemplateOption `json:"options"`
	// Use flag for options.
	UseOptions *bool `json:"useOptions,omitempty"`
	// Type of server that will serve ranges created from this template.
	ServerAssociationType string `json:"serverAssociationType,omitempty"`
	// Failover association name.
	FailoverAssociation *string `json:"failoverAssociation,omitempty"`
	// Grid member that will serve ranges created from this template.
	Member *RangeTemplateMember `json:"member,omitempty"`
	// Whether this template can be used in cloud-computing deployments.
	CloudApiCompatible *bool `json:"cloudApiCompatible,omitempty"`
	// Microsoft DHCP server name (write-only convenience parameter). The SDK wraps this into a nested Msdhcpserver struct persisted on the response as ms_server — a different shape than this flat field, so it is never echoed back and excluded from the AtProvider mirror.
	MsServer *string `json:"msServer,omitempty"`
}

// RangeTemplateObservation holds the observed state of a RangeTemplate.
// It mirrors every RangeTemplateParameters field plus response-only fields
// (full-mirror AtProvider) so users can compare forProvider to
// atProvider before switching to full management
// (managementPolicies: ["Observe"]).
type RangeTemplateObservation struct {
	// Server-assigned external identifier for this resource, set by Observe()
	// from the resource's external name. Used by import testing to confirm
	// the controller re-discovers the resource after a restart.
	// +optional
	ID string `json:"id,omitempty"` // atProvider
	// Name of the range template object.
	Name *string `json:"name,omitempty"` // atProvider
	// Number of addresses the range should contain when instantiated.
	NumberOfAddresses *uint32 `json:"numberOfAddresses,omitempty"` // atProvider
	// Start address offset within the parent network when instantiated.
	Offset *uint32 `json:"offset,omitempty"` // atProvider
	// Comment for the range template.
	Comment *string `json:"comment,omitempty"` // atProvider
	// Extensible attributes (arbitrary key/value metadata defined in Grid Manager). The WAPI wire format wraps each value as {"value": ...}; this map is the simplified string-valued CRD representation (the controller translates to/from the SDK's EA map[string]interface{} type).
	// +optional
	ExtAttrs map[string]string `json:"extAttrs"` // atProvider
	// DHCP options to apply to ranges created from this template.
	// +optional
	Options []*RangeTemplateOption `json:"options"` // atProvider
	// Use flag for options.
	UseOptions *bool `json:"useOptions,omitempty"` // atProvider
	// Type of server that will serve ranges created from this template.
	ServerAssociationType *string `json:"serverAssociationType,omitempty"` // atProvider
	// Failover association name.
	FailoverAssociation *string `json:"failoverAssociation,omitempty"` // atProvider
	// Grid member that will serve ranges created from this template.
	Member *RangeTemplateMember `json:"member,omitempty"` // atProvider
	// Whether this template can be used in cloud-computing deployments.
	CloudApiCompatible *bool `json:"cloudApiCompatible,omitempty"` // atProvider
	// Server-assigned opaque object reference (WAPI `_ref`). Mirrors the crossplane.io/external-name annotation for observability and uptest import verification.
	Ref *string `json:"ref,omitempty"` // atProvider
}

// RangeTemplateSpec defines the desired state of RangeTemplate.
type RangeTemplateSpec struct {
	xpv2.ManagedResourceSpec `json:",inline"`

	// DriftDetection configures which forProvider fields are owned
	// outside Crossplane and how drift in those fields is detected and
	// corrected. Absent configuration means drift detection is enabled
	// with no ignored paths -- today's behaviour.
	// +optional
	DriftDetection *driftdetection.DriftDetection `json:"driftDetection,omitempty"`

	// ForProvider holds the user-supplied parameters for this RangeTemplate.
	// +kubebuilder:validation:Required
	ForProvider RangeTemplateParameters `json:"forProvider"`
}

// RangeTemplateStatus defines the observed state of RangeTemplate.
type RangeTemplateStatus struct {
	xpv2.ManagedResourceStatus `json:",inline"`

	// AtProvider holds the observed values from the Infoblox NIOS WAPI.
	// +optional
	AtProvider RangeTemplateObservation `json:"atProvider,omitempty"`
}

// +kubebuilder:object:root=true
// +kubebuilder:storageversion
// +kubebuilder:subresource:status
// +kubebuilder:resource:scope=Namespaced,categories={crossplane,managed,infobloxnios}
// +kubebuilder:printcolumn:name="READY",type="string",JSONPath=".status.conditions[?(@.type=='Ready')].status"
// +kubebuilder:printcolumn:name="SYNCED",type="string",JSONPath=".status.conditions[?(@.type=='Synced')].status"
// +kubebuilder:printcolumn:name="EXTERNAL-NAME",type="string",JSONPath=".metadata.annotations.crossplane\\.io/external-name"
// +kubebuilder:printcolumn:name="AGE",type="date",JSONPath=".metadata.creationTimestamp"

// RangeTemplate is the Namespaced-scoped managed resource representing an Infoblox NIOS RangeTemplate.
type RangeTemplate struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`

	Spec   RangeTemplateSpec   `json:"spec"`
	Status RangeTemplateStatus `json:"status,omitempty"`
}

// +kubebuilder:object:root=true

// RangeTemplateList contains a list of RangeTemplate.
type RangeTemplateList struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []RangeTemplate `json:"items"`
}
