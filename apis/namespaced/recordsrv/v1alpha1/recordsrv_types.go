// Code generated by openapi2crd. DO NOT EDIT.

// Package v1alpha1 contains the Namespaced-scoped SRVRecord managed resource.
// API group: recordsrv.infobloxnios.m.crossplane.io
package v1alpha1

import (
	xpv1 "github.com/crossplane/crossplane-runtime/v2/apis/common/v1"
	xpv2 "github.com/crossplane/crossplane-runtime/v2/apis/common/v2"
	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
)

// SRVRecordAwsRte53RecordInfo carries AWS Route 53 record information for a cloud-managed SRVRecord (mirrors the SDK's Awsrte53recordinfo struct).
type SRVRecordAwsRte53RecordInfo struct {
	// DNS name of the alias target.
	AliasTargetDnsName *string `json:"aliasTargetDnsName,omitempty"`
	// Hosted zone ID of the alias target.
	AliasTargetHostedZoneId *string `json:"aliasTargetHostedZoneId,omitempty"`
	// Indicates if Amazon Route 53 evaluates the health of the alias target.
	AliasTargetEvaluateTargetHealth *bool `json:"aliasTargetEvaluateTargetHealth,omitempty"`
	// Indicates whether this is the primary or secondary resource record for Amazon Route 53 failover routing.
	Failover *string `json:"failover,omitempty"`
	// Continent code for Amazon Route 53 geolocation routing.
	GeolocationContinentCode *string `json:"geolocationContinentCode,omitempty"`
	// Country code for Amazon Route 53 geolocation routing.
	GeolocationCountryCode *string `json:"geolocationCountryCode,omitempty"`
	// Subdivision code for Amazon Route 53 geolocation routing.
	GeolocationSubdivisionCode *string `json:"geolocationSubdivisionCode,omitempty"`
	// ID of the health check that Amazon Route 53 performs for this resource record.
	HealthCheckId *string `json:"healthCheckId,omitempty"`
	// Amazon EC2 region where this resource record resides for latency routing.
	Region *string `json:"region,omitempty"`
	// An identifier that differentiates records with the same DNS name and type for weighted, latency, geolocation, and failover routing.
	SetIdentifier *string `json:"setIdentifier,omitempty"`
	// Type of Amazon Route 53 resource record.
	Type *string `json:"type,omitempty"`
	// Value that determines the portion of traffic for this record in weighted routing. The range is from 0 to 255.
	Weight *uint32 `json:"weight,omitempty"`
}

// SRVRecordCloudInfo carries Cloud API delegation/ownership information for a cloud-managed SRVRecord (mirrors the SDK's GridCloudapiInfo struct).
type SRVRecordCloudInfo struct {
	// The Cloud Platform Appliance to which authority of the object is delegated.
	DelegatedMember *SRVRecordCloudInfoDelegatedMember `json:"delegatedMember,omitempty"`
	// Indicates the scope of delegation for the object. This can be one of the following: NONE (outside any delegation), ROOT (the delegation point), SUBTREE (within the scope of a delegation), RECLAIMING (within the scope of a delegation being reclaimed, either as the delegation point or in the subtree).
	DelegatedScope *string `json:"delegatedScope,omitempty"`
	// Indicates the root of the delegation if delegated_scope is SUBTREE or RECLAIMING. This is not set otherwise.
	DelegatedRoot *string `json:"delegatedRoot,omitempty"`
	// Determines whether the object was created by the cloud adapter or not.
	OwnedByAdaptor *bool `json:"ownedByAdaptor,omitempty"`
	// Indicates the cloud origin of the object.
	Usage *string `json:"usage,omitempty"`
	// Reference to the tenant object associated with the object, if any.
	Tenant *string `json:"tenant,omitempty"`
	// Indicates the specified cloud management platform.
	MgmtPlatform *string `json:"mgmtPlatform,omitempty"`
	// Type of authority over the object.
	AuthorityType *string `json:"authorityType,omitempty"`
}

// SRVRecordCloudInfoDelegatedMember identifies the Grid member a cloud-managed SRVRecord's authority is delegated to (mirrors the SDK's Dhcpmember struct).
type SRVRecordCloudInfoDelegatedMember struct {
	// The IPv4 Address of the Grid Member.
	Ipv4Addr *string `json:"ipv4addr,omitempty"`
	// The IPv6 Address of the Grid Member.
	Ipv6Addr *string `json:"ipv6addr,omitempty"`
	// The Grid member name
	Name *string `json:"name,omitempty"`
}

// SRVRecordMsAdUserData carries Microsoft Active Directory user information for an MS-managed SRVRecord (mirrors the SDK's MsserverAduserData struct).
type SRVRecordMsAdUserData struct {
	// The number of active users.
	ActiveUsersCount *uint32 `json:"activeUsersCount,omitempty"`
}

// SRVRecordParameters are the configurable fields of a SRVRecord.
type SRVRecordParameters struct {
	// Owner name in FQDN format the SRV record applies to. Changing this changes the record's _ref.
	// +kubebuilder:validation:Required
	Name *string `json:"name"`
	// Target host in FQDN format. Changing this changes the record's _ref.
	// +kubebuilder:validation:Required
	Target *string `json:"target"`
	// Priority of the record (0-65535) — lower values are preferred. Changing this changes the record's _ref.
	// +kubebuilder:validation:Required
	Priority *uint32 `json:"priority"`
	// Relative weight (0-65535) for records with the same priority. Changing this changes the record's _ref.
	// +kubebuilder:validation:Required
	Weight *uint32 `json:"weight"`
	// TCP/UDP port (0-65535) on the target host. Changing this changes the record's _ref.
	// +kubebuilder:validation:Required
	Port *uint32 `json:"port"`
	// Comment for the record; maximum 256 characters.
	Comment *string `json:"comment,omitempty"`
	// Time-to-live in seconds. Zero means the record is not cached. Must be non-negative (0-2147483647); to inherit the zone/grid default, set useTtl to false rather than passing a negative sentinel value.
	// +kubebuilder:validation:Minimum=0
	// +kubebuilder:validation:Maximum=2147483647
	TTL *uint32 `json:"ttl,omitempty"`
	// Use flag for ttl — when false the zone/grid default TTL applies.
	UseTTL *bool `json:"useTtl,omitempty"`
	// Extensible attributes (arbitrary key/value metadata defined in Grid Manager). The WAPI wire format wraps each value as {"value": ...}; this map is the simplified string-valued CRD representation (the controller translates to/from the SDK's EA map[string]interface{} type).
	// +optional
	// +kubebuilder:validation:XValidation:rule="self.all(k, k != 'Crossplane Internal ID')",message="the 'Crossplane Internal ID' extensible attribute is reserved for the provider's identity stamp and cannot be set in spec.forProvider.extAttrs"
	ExtAttrs map[string]string `json:"extAttrs"`
	// DNS view in which the record resides, e.g. "external". Fixed at creation — WAPI ties the record's _ref to (view, zone, name). Confirmed absent from the UpdateSRVRecord SDK method signature.
	// +kubebuilder:validation:Required
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="view is immutable after creation"
	View *string `json:"view"`
}

// SRVRecordObservation holds the observed state of a SRVRecord.
// It mirrors every SRVRecordParameters field plus response-only fields
// (full-mirror AtProvider) so users can compare forProvider to
// atProvider before switching to full management
// (managementPolicies: ["Observe"]).
type SRVRecordObservation struct {
	// Server-assigned external identifier for this resource, set by Observe()
	// from the resource's external name. Used by import testing to confirm
	// the controller re-discovers the resource after a restart.
	// +optional
	ID string `json:"id,omitempty"` // atProvider
	// Owner name in FQDN format the SRV record applies to. Changing this changes the record's _ref.
	Name *string `json:"name,omitempty"` // atProvider
	// Target host in FQDN format. Changing this changes the record's _ref.
	Target *string `json:"target,omitempty"` // atProvider
	// Priority of the record (0-65535) — lower values are preferred. Changing this changes the record's _ref.
	Priority *uint32 `json:"priority,omitempty"` // atProvider
	// Relative weight (0-65535) for records with the same priority. Changing this changes the record's _ref.
	Weight *uint32 `json:"weight,omitempty"` // atProvider
	// TCP/UDP port (0-65535) on the target host. Changing this changes the record's _ref.
	Port *uint32 `json:"port,omitempty"` // atProvider
	// Comment for the record; maximum 256 characters.
	Comment *string `json:"comment,omitempty"` // atProvider
	// Time-to-live in seconds. Zero means the record is not cached. Must be non-negative (0-2147483647); to inherit the zone/grid default, set useTtl to false rather than passing a negative sentinel value.
	TTL *uint32 `json:"ttl,omitempty"` // atProvider
	// Use flag for ttl — when false the zone/grid default TTL applies.
	UseTTL *bool `json:"useTtl,omitempty"` // atProvider
	// Extensible attributes (arbitrary key/value metadata defined in Grid Manager). The WAPI wire format wraps each value as {"value": ...}; this map is the simplified string-valued CRD representation (the controller translates to/from the SDK's EA map[string]interface{} type).
	// +optional
	ExtAttrs map[string]string `json:"extAttrs"` // atProvider
	// DNS view in which the record resides, e.g. "external". Fixed at creation — WAPI ties the record's _ref to (view, zone, name). Confirmed absent from the UpdateSRVRecord SDK method signature.
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="view is immutable after creation"
	View *string `json:"view,omitempty"` // atProvider
	// Server-assigned opaque object reference (WAPI `_ref`). Mirrors the crossplane.io/external-name annotation for observability and uptest import verification.
	Ref *string `json:"ref,omitempty"` // atProvider
	// Zone in which the record resides, e.g. "zone.com". Derived from name/view by WAPI — not a CreateSRVRecord parameter, so it has no ForProvider counterpart.
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="zone is immutable after creation"
	Zone *string `json:"zone,omitempty"` // atProvider
	// Record name in punycode format (derived from name).
	DNSName *string `json:"dnsName,omitempty"` // atProvider
	// Target host in punycode format (derived from target).
	DNSTarget *string `json:"dnsTarget,omitempty"` // atProvider
	// Record creator. Changing to/from 'SYSTEM' is not allowed.
	Creator *string `json:"creator,omitempty"` // atProvider
	// Record creation time (Unix epoch seconds).
	CreationTime *int64 `json:"creationTime,omitempty"` // atProvider
	// Time of the last DNS query for this record (Unix epoch seconds).
	LastQueried *int64 `json:"lastQueried,omitempty"` // atProvider
	// GSS-TSIG principal that owns this record (DDNS-created records only).
	DdnsPrincipal *string `json:"ddnsPrincipal,omitempty"` // atProvider
	// Whether DDNS updates for this record are protected.
	DdnsProtected *bool `json:"ddnsProtected,omitempty"` // atProvider
	// Whether reclamation is forbidden for the record (DNS discovery feature).
	ForbidReclamation *bool `json:"forbidReclamation,omitempty"` // atProvider
	// Whether the record is reclaimable (DNS discovery feature).
	Reclaimable *bool `json:"reclaimable,omitempty"` // atProvider
	// Name of the shared record group, if this is a shared record.
	SharedRecordGroup *string `json:"sharedRecordGroup,omitempty"` // atProvider
	// AWS Route 53 record information (cloud-managed records only).
	AwsRte53RecordInfo *SRVRecordAwsRte53RecordInfo `json:"awsRte53RecordInfo,omitempty"` // atProvider
	// Cloud API related information for this object (cloud-managed records only).
	CloudInfo *SRVRecordCloudInfo `json:"cloudInfo,omitempty"` // atProvider
	// Microsoft Active Directory user information (MS-managed records only).
	MsAdUserData *SRVRecordMsAdUserData `json:"msAdUserData,omitempty"` // atProvider
}

// SRVRecordSpec defines the desired state of SRVRecord.
type SRVRecordSpec struct {
	xpv2.ManagedResourceSpec `json:",inline"`

	// ForProvider holds the user-supplied parameters for this SRVRecord.
	// +kubebuilder:validation:Required
	ForProvider SRVRecordParameters `json:"forProvider"`
}

// SRVRecordStatus defines the observed state of SRVRecord.
type SRVRecordStatus struct {
	xpv1.ResourceStatus `json:",inline"`

	// AtProvider holds the observed values from the Infoblox NIOS WAPI.
	// +optional
	AtProvider SRVRecordObservation `json:"atProvider,omitempty"`
}

// +kubebuilder:object:root=true
// +kubebuilder:storageversion
// +kubebuilder:subresource:status
// +kubebuilder:resource:scope=Namespaced,categories={crossplane,managed,infobloxnios}
// +kubebuilder:printcolumn:name="READY",type="string",JSONPath=".status.conditions[?(@.type=='Ready')].status"
// +kubebuilder:printcolumn:name="SYNCED",type="string",JSONPath=".status.conditions[?(@.type=='Synced')].status"
// +kubebuilder:printcolumn:name="EXTERNAL-NAME",type="string",JSONPath=".metadata.annotations.crossplane\\.io/external-name"
// +kubebuilder:printcolumn:name="AGE",type="date",JSONPath=".metadata.creationTimestamp"

// SRVRecord is the Namespaced-scoped managed resource representing an Infoblox NIOS SRVRecord.
type SRVRecord struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`

	Spec   SRVRecordSpec   `json:"spec"`
	Status SRVRecordStatus `json:"status,omitempty"`
}

// +kubebuilder:object:root=true

// SRVRecordList contains a list of SRVRecord.
type SRVRecordList struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []SRVRecord `json:"items"`
}
