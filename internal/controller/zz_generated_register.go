// Code generated by hack/generate-registration.go. DO NOT EDIT.
//
// SetupGated defers MR controller startup until CRDs are installed (SafeStart).
// Setup starts all controllers immediately (RBAC fallback path).
// ProviderConfig (config.Setup) is NEVER gated — it must always be available.
// See the gated controller registration convention (SafeStart pattern).

// SPDX-FileCopyrightText: 2025 The Crossplane Authors <https://crossplane.io>
//
// SPDX-License-Identifier: Apache-2.0

package controller

import (
	"github.com/crossplane-contrib/provider-infoblox-nios/internal/controller/config"
	"github.com/crossplane-contrib/provider-infoblox-nios/internal/controller/recorda"
	"github.com/crossplane-contrib/provider-infoblox-nios/internal/controller/recordtxt"
	"github.com/crossplane-contrib/provider-infoblox-nios/internal/controller/zonedelegated"
	"github.com/crossplane/crossplane-runtime/v2/pkg/controller"
	ctrl "sigs.k8s.io/controller-runtime"
)

// SetupGated creates all controllers with safe-start (gate) support.
// MR controllers defer startup until CRDs are installed.
func SetupGated(mgr ctrl.Manager, o controller.Options) error {
	for _, setup := range []func(ctrl.Manager, controller.Options) error{
		// ProviderConfig — never gated, must always be available.
		config.Setup,
		recorda.SetupGated,
		recordtxt.SetupGated,
		zonedelegated.SetupGated,
	} {
		if err := setup(mgr, o); err != nil {
			return err
		}
	}
	return nil
}

// Setup creates all controllers without gate support (RBAC fallback).
// MR controllers start immediately without waiting for CRD installation.
func Setup(mgr ctrl.Manager, o controller.Options) error {
	for _, setup := range []func(ctrl.Manager, controller.Options) error{
		// ProviderConfig — never gated, must always be available.
		config.Setup,
		recorda.Setup,
		recordtxt.Setup,
		zonedelegated.Setup,
	} {
		if err := setup(mgr, o); err != nil {
			return err
		}
	}
	return nil
}
