---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dtclbdns.dtclbdn.infobloxnios.m.crossplane.io
spec:
  group: dtclbdn.infobloxnios.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - infobloxnios
    kind: DTCLBDN
    listKind: DTCLBDNList
    plural: dtclbdns
    singular: dtclbdn
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DTCLBDN is the Namespaced-scoped managed resource representing
          an Infoblox NIOS DTCLBDN.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DTCLBDNSpec defines the desired state of DTCLBDN.
            properties:
              forProvider:
                description: ForProvider holds the user-supplied parameters for this
                  DTCLBDN.
                properties:
                  authZones:
                    description: 'Authoritative zones the LBDN''s patterns are matched
                      against, identified by each ZoneAuth''s WAPI `_ref`. Live-verified:
                      WAPI requires bare _ref strings, not objects. Linked zones must
                      have a Grid primary configured.'
                    items:
                      type: string
                    type: array
                  authZonesRefs:
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  authZonesSelector:
                    description: NamespacedSelector selects a namespaced object.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  comment:
                    description: Comment for the DTC LBDN; maximum 256 characters.
                    type: string
                  disable:
                    description: Whether the DTC LBDN is disabled.
                    type: boolean
                  extAttrs:
                    additionalProperties:
                      type: string
                    description: 'Extensible attributes (arbitrary key/value metadata
                      defined in Grid Manager). The WAPI wire format wraps each value
                      as {"value": ...}; this map is the simplified string-valued
                      CRD representation (the controller translates to/from the SDK''s
                      EA map[string]interface{} type).'
                    type: object
                    x-kubernetes-validations:
                    - message: the 'Crossplane Internal ID' extensible attribute is
                        reserved for the provider's identity stamp and cannot be set
                        in spec.forProvider.extAttrs
                      rule: self.all(k, k != 'Crossplane Internal ID')
                  lbMethod:
                    description: Load balancing method used to select a pool from
                      those listed in pools.
                    enum:
                    - ROUND_ROBIN
                    - RATIO
                    - TOPOLOGY
                    - GLOBAL_AVAILABILITY
                    type: string
                  name:
                    description: DTC LBDN (Load Balanced Domain Name) display name
                      (not DNS-related). Mutable — changes the WAPI `_ref`.
                    type: string
                  patterns:
                    description: FQDN wildcard patterns matched against incoming queries.
                      At least one pattern is required for the LBDN to answer any
                      query.
                    items:
                      type: string
                    type: array
                  persistence:
                    description: Seconds to cache a client-specific LBDN response.
                      Zero disables caching.
                    format: int32
                    type: integer
                  pools:
                    description: Pools associated with this LBDN, each with a per-pool
                      match priority ratio.
                    items:
                      description: DTCLBDNPoolLink identifies one DTCPool member of
                        a DTCLBDN along with its match priority ratio (mirrors the
                        SDK's DtcPoolLink struct).
                      properties:
                        pool:
                          description: DTCPool member of the LBDN, identified by its
                            Crossplane external-name (the DTCPool's WAPI `_ref`).
                          type: string
                        poolRef:
                          description: A NamespacedReference to a named object.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        poolSelector:
                          description: NamespacedSelector selects a namespaced object.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        ratio:
                          description: The match priority weight of the pool within
                            the LBDN.
                          format: int32
                          type: integer
                      type: object
                    type: array
                  priority:
                    description: Match priority among overlapping LBDNs (lower ordinal
                      wins).
                    format: int32
                    type: integer
                  topology:
                    description: Topology ruleset name used when lbMethod=TOPOLOGY.
                    type: string
                  ttl:
                    description: Time-To-Live for the DTC LBDN response, in seconds.
                      Must be non-negative (0-2147483647); to inherit the zone/grid
                      default, set useTtl to false rather than passing a negative
                      sentinel value.
                    format: int32
                    maximum: 2147483647
                    minimum: 0
                    type: integer
                  types:
                    description: Resource record types the LBDN answers with.
                    items:
                      enum:
                      - A
                      - AAAA
                      - CNAME
                      - NAPTR
                      - SRV
                      type: string
                    type: array
                  useTtl:
                    description: Use flag for ttl.
                    type: boolean
                required:
                - lbMethod
                - name
                - patterns
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: DTCLBDNStatus defines the observed state of DTCLBDN.
            properties:
              atProvider:
                description: AtProvider holds the observed values from the Infoblox
                  NIOS WAPI.
                properties:
                  authZones:
                    description: 'Authoritative zones the LBDN''s patterns are matched
                      against, identified by each ZoneAuth''s WAPI `_ref`. Live-verified:
                      WAPI requires bare _ref strings, not objects. Linked zones must
                      have a Grid primary configured.'
                    items:
                      type: string
                    type: array
                  comment:
                    description: Comment for the DTC LBDN; maximum 256 characters.
                    type: string
                  disable:
                    description: Whether the DTC LBDN is disabled.
                    type: boolean
                  extAttrs:
                    additionalProperties:
                      type: string
                    description: 'Extensible attributes (arbitrary key/value metadata
                      defined in Grid Manager). The WAPI wire format wraps each value
                      as {"value": ...}; this map is the simplified string-valued
                      CRD representation (the controller translates to/from the SDK''s
                      EA map[string]interface{} type).'
                    type: object
                  health:
                    description: Aggregate health status of the LBDN.
                    properties:
                      availability:
                        description: The availability color status.
                        type: string
                      description:
                        description: The textual description of the object's status.
                        type: string
                      enabledState:
                        description: The enabled state of the object.
                        type: string
                    type: object
                  id:
                    description: |-
                      Server-assigned external identifier for this resource, set by Observe()
                      from the resource's external name. Used by import testing to confirm
                      the controller re-discovers the resource after a restart.
                    type: string
                  lbMethod:
                    description: Load balancing method used to select a pool from
                      those listed in pools.
                    type: string
                  name:
                    description: DTC LBDN (Load Balanced Domain Name) display name
                      (not DNS-related). Mutable — changes the WAPI `_ref`.
                    type: string
                  patterns:
                    description: FQDN wildcard patterns matched against incoming queries.
                      At least one pattern is required for the LBDN to answer any
                      query.
                    items:
                      type: string
                    type: array
                  persistence:
                    description: Seconds to cache a client-specific LBDN response.
                      Zero disables caching.
                    format: int32
                    type: integer
                  pools:
                    description: Pools associated with this LBDN, each with a per-pool
                      match priority ratio.
                    items:
                      description: DTCLBDNPoolLink identifies one DTCPool member of
                        a DTCLBDN along with its match priority ratio (mirrors the
                        SDK's DtcPoolLink struct).
                      properties:
                        pool:
                          description: DTCPool member of the LBDN, identified by its
                            Crossplane external-name (the DTCPool's WAPI `_ref`).
                          type: string
                        poolRef:
                          description: A NamespacedReference to a named object.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        poolSelector:
                          description: NamespacedSelector selects a namespaced object.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        ratio:
                          description: The match priority weight of the pool within
                            the LBDN.
                          format: int32
                          type: integer
                      type: object
                    type: array
                  priority:
                    description: Match priority among overlapping LBDNs (lower ordinal
                      wins).
                    format: int32
                    type: integer
                  ref:
                    description: Server-assigned opaque object reference (WAPI `_ref`).
                      Mirrors the crossplane.io/external-name annotation for observability
                      and uptest import verification.
                    type: string
                  topology:
                    description: Topology ruleset name used when lbMethod=TOPOLOGY.
                    type: string
                  ttl:
                    description: Time-To-Live for the DTC LBDN response, in seconds.
                      Must be non-negative (0-2147483647); to inherit the zone/grid
                      default, set useTtl to false rather than passing a negative
                      sentinel value.
                    format: int32
                    type: integer
                  types:
                    description: Resource record types the LBDN answers with.
                    items:
                      type: string
                    type: array
                  useTtl:
                    description: Use flag for ttl.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
