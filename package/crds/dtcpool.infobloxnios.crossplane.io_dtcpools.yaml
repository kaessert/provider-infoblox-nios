---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: dtcpools.dtcpool.infobloxnios.crossplane.io
spec:
  group: dtcpool.infobloxnios.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - infobloxnios
    kind: DTCPool
    listKind: DTCPoolList
    plural: dtcpools
    singular: dtcpool
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DTCPool is the Cluster-scoped managed resource representing an
          Infoblox NIOS DTCPool.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DTCPoolSpec defines the desired state of DTCPool.
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              driftDetection:
                description: |-
                  DriftDetection configures which forProvider fields are owned
                  outside Crossplane and how drift in those fields is detected and
                  corrected. Absent configuration means drift detection is enabled
                  with no ignored paths -- today's behaviour.
                properties:
                  ignore:
                    description: Ignore declares fields owned outside Crossplane.
                    items:
                      description: |-
                        IgnoreRule declares fields whose value is owned by something other than
                        Crossplane. Listed fields are seeded from spec on create, then neither
                        compared nor written from spec: the value observed on the external
                        resource is carried forward instead.
                      properties:
                        paths:
                          description: |-
                            Paths to ignore, in Crossplane field path notation rooted at the
                            managed resource spec, for example forProvider.comment. This is the
                            grammar Composition patches use; JSON Pointer form
                            (/forProvider/comment) is also accepted. List indices and wildcards
                            are not supported -- ignore the whole list instead.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  mode:
                    default: enabled
                    description: Mode selects the drift detection behaviour.
                    enum:
                    - enabled
                    - warn
                    - disabled
                    type: string
                type: object
              forProvider:
                description: ForProvider holds the user-supplied parameters for this
                  DTCPool.
                properties:
                  availability:
                    description: 'Determines when a pool member is considered available:
                      ANY, at least QUORUM, or ALL of its monitors report it up.'
                    enum:
                    - ANY
                    - QUORUM
                    - ALL
                    type: string
                  comment:
                    description: Comment for the DTC Pool; maximum 256 characters.
                    type: string
                  disable:
                    description: Whether the DTC Pool is disabled.
                    type: boolean
                  extAttrs:
                    additionalProperties:
                      type: string
                    description: 'Extensible attributes (arbitrary key/value metadata
                      defined in Grid Manager). The WAPI wire format wraps each value
                      as {"value": ...}; this map is the simplified string-valued
                      CRD representation (the controller translates to/from the SDK''s
                      EA map[string]interface{} type).'
                    type: object
                    x-kubernetes-validations:
                    - message: the 'Crossplane Internal ID' extensible attribute is
                        reserved for the provider's identity stamp and cannot be set
                        in spec.forProvider.extAttrs
                      rule: self.all(k, k != 'Crossplane Internal ID')
                  lbAlternateMethod:
                    description: Fallback load balancing method used if lbPreferredMethod
                      returns no results. NONE is accepted here for backward compatibility
                      with existing manifests, but the backend never accepts it as
                      an explicit write value — it only appears on read-back as the
                      server's default when the field is left unset, and the controller
                      normalizes it to "unset" on both the read and write paths.
                    enum:
                    - NONE
                    - ROUND_ROBIN
                    - RATIO
                    - TOPOLOGY
                    - DYNAMIC_RATIO
                    - GLOBAL_AVAILABILITY
                    - ALL_AVAILABLE
                    type: string
                  lbAlternateTopology:
                    description: Topology ruleset name used when lbAlternateMethod=TOPOLOGY.
                    type: string
                  lbDynamicRatioAlternate:
                    description: Dynamic-ratio settings used when lbAlternateMethod=DYNAMIC_RATIO.
                    properties:
                      invertMonitorMetric:
                        description: Whether the inverted values of the DTC SNMP monitor
                          metric are used.
                        type: boolean
                      method:
                        description: The method of the DTC dynamic ratio load balancing.
                        type: string
                      monitor:
                        description: The DTC monitor whose output is used for dynamic
                          ratio load balancing.
                        type: string
                      monitorMetric:
                        description: The metric of the DTC SNMP monitor used for dynamic
                          weighing.
                        type: string
                      monitorWeighing:
                        description: 'How the DTC monitor weight is applied: PRIORITY
                          forwards clients to the least loaded server; RATIO calculates
                          distribution from dynamic weights.'
                        type: string
                    type: object
                  lbDynamicRatioPreferred:
                    description: Dynamic-ratio settings used when lbPreferredMethod=DYNAMIC_RATIO.
                    properties:
                      invertMonitorMetric:
                        description: Whether the inverted values of the DTC SNMP monitor
                          metric are used.
                        type: boolean
                      method:
                        description: The method of the DTC dynamic ratio load balancing.
                        type: string
                      monitor:
                        description: The DTC monitor whose output is used for dynamic
                          ratio load balancing.
                        type: string
                      monitorMetric:
                        description: The metric of the DTC SNMP monitor used for dynamic
                          weighing.
                        type: string
                      monitorWeighing:
                        description: 'How the DTC monitor weight is applied: PRIORITY
                          forwards clients to the least loaded server; RATIO calculates
                          distribution from dynamic weights.'
                        type: string
                    type: object
                  lbPreferredMethod:
                    description: Preferred load balancing method used to select a
                      server from the pool.
                    enum:
                    - ROUND_ROBIN
                    - RATIO
                    - TOPOLOGY
                    - DYNAMIC_RATIO
                    - GLOBAL_AVAILABILITY
                    - ALL_AVAILABLE
                    type: string
                  lbPreferredTopology:
                    description: Topology ruleset name used when lbPreferredMethod=TOPOLOGY.
                    type: string
                  monitors:
                    description: Health monitors associated with the pool.
                    items:
                      description: DTCPoolMonitor identifies a health monitor associated
                        with a DTCPool by its WAPI `_ref` (mirrors the SDK's polymorphic
                        Monitor interface, simplified to a reference — no monitor
                        sub-type is independently cataloged today).
                      properties:
                        monitor:
                          description: Reference (`_ref`) of the monitor associated
                            with the pool, e.g. "dtc:monitor:http/ZG5z...".
                          type: string
                      type: object
                    type: array
                  name:
                    description: DTC Pool display name. Mutable, but changes are not
                      reference-resolved on the fly by referencing DTCLBDN resources
                      — see the DTCLBDN resource note when it is onboarded.
                    type: string
                  quorum:
                    description: Minimum number of monitors that must report a resource
                      as up for it to be available. Only meaningful when availability=QUORUM.
                    format: int32
                    type: integer
                  servers:
                    description: DTC Servers that are members of this pool, each with
                      a per-server ratio/weight.
                    items:
                      description: DTCPoolServerLink identifies one DTCServer member
                        of a DTCPool along with its load-balancing weight (mirrors
                        the SDK's DtcServerLink struct).
                      properties:
                        ratio:
                          description: The load-balancing weight of the server within
                            the pool.
                          format: int32
                          type: integer
                        server:
                          description: DTCServer member of the pool, identified by
                            its Crossplane external-name (the DTCServer's WAPI `_ref`).
                          type: string
                        serverRef:
                          description: A Reference to a named object.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        serverSelector:
                          description: A Selector selects an object.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  ttl:
                    description: Time-To-Live for the DTC Pool response, in seconds.
                      Zero indicates the record should not be cached. Must be non-negative
                      (0-2147483647); to inherit the zone/grid default, set useTtl
                      to false rather than passing a negative sentinel value.
                    format: int32
                    maximum: 2147483647
                    minimum: 0
                    type: integer
                  useTtl:
                    description: Use flag for ttl.
                    type: boolean
                required:
                - lbPreferredMethod
                - name
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: DTCPoolStatus defines the observed state of DTCPool.
            properties:
              atProvider:
                description: AtProvider holds the observed values from the Infoblox
                  NIOS WAPI.
                properties:
                  availability:
                    description: 'Determines when a pool member is considered available:
                      ANY, at least QUORUM, or ALL of its monitors report it up.'
                    type: string
                  comment:
                    description: Comment for the DTC Pool; maximum 256 characters.
                    type: string
                  consolidatedMonitors:
                    description: Consolidated monitor health status shared across
                      pool members. Read-only — Update accepts a differently-shaped
                      payload for this field via the SDK's generic consolidatedMonitors
                      parameter, not this typed representation.
                    items:
                      description: DTCPoolConsolidatedMonitorHealth carries consolidated
                        monitor health status shared across pool members (mirrors
                        the SDK's DtcPoolConsolidatedMonitorHealth struct).
                      properties:
                        availability:
                          description: Servers assigned to the pool with this monitor
                            are healthy if ANY or ALL members report a healthy status.
                          type: string
                        fullHealthCommunication:
                          description: Whether health checks are performed on each
                            DTC Grid member serving related LBDN(s) and shared across
                            all DTC Grid members, not just the selected list.
                          type: boolean
                        members:
                          description: Members whose monitor statuses are shared across
                            other members in the pool.
                          items:
                            type: string
                          type: array
                        monitor:
                          description: Monitor whose statuses are shared across other
                            members in the pool.
                          type: string
                      type: object
                    type: array
                  disable:
                    description: Whether the DTC Pool is disabled.
                    type: boolean
                  extAttrs:
                    additionalProperties:
                      type: string
                    description: 'Extensible attributes (arbitrary key/value metadata
                      defined in Grid Manager). The WAPI wire format wraps each value
                      as {"value": ...}; this map is the simplified string-valued
                      CRD representation (the controller translates to/from the SDK''s
                      EA map[string]interface{} type).'
                    type: object
                  health:
                    description: Aggregate health status of the pool.
                    properties:
                      availability:
                        description: The availability color status.
                        type: string
                      description:
                        description: The textual description of the object's status.
                        type: string
                      enabledState:
                        description: The enabled state of the object.
                        type: string
                    type: object
                  id:
                    description: |-
                      Server-assigned external identifier for this resource, set by Observe()
                      from the resource's external name. Used by import testing to confirm
                      the controller re-discovers the resource after a restart.
                    type: string
                  lbAlternateMethod:
                    description: Fallback load balancing method used if lbPreferredMethod
                      returns no results. NONE is accepted here for backward compatibility
                      with existing manifests, but the backend never accepts it as
                      an explicit write value — it only appears on read-back as the
                      server's default when the field is left unset, and the controller
                      normalizes it to "unset" on both the read and write paths.
                    type: string
                  lbAlternateTopology:
                    description: Topology ruleset name used when lbAlternateMethod=TOPOLOGY.
                    type: string
                  lbDynamicRatioAlternate:
                    description: Dynamic-ratio settings used when lbAlternateMethod=DYNAMIC_RATIO.
                    properties:
                      invertMonitorMetric:
                        description: Whether the inverted values of the DTC SNMP monitor
                          metric are used.
                        type: boolean
                      method:
                        description: The method of the DTC dynamic ratio load balancing.
                        type: string
                      monitor:
                        description: The DTC monitor whose output is used for dynamic
                          ratio load balancing.
                        type: string
                      monitorMetric:
                        description: The metric of the DTC SNMP monitor used for dynamic
                          weighing.
                        type: string
                      monitorWeighing:
                        description: 'How the DTC monitor weight is applied: PRIORITY
                          forwards clients to the least loaded server; RATIO calculates
                          distribution from dynamic weights.'
                        type: string
                    type: object
                  lbDynamicRatioPreferred:
                    description: Dynamic-ratio settings used when lbPreferredMethod=DYNAMIC_RATIO.
                    properties:
                      invertMonitorMetric:
                        description: Whether the inverted values of the DTC SNMP monitor
                          metric are used.
                        type: boolean
                      method:
                        description: The method of the DTC dynamic ratio load balancing.
                        type: string
                      monitor:
                        description: The DTC monitor whose output is used for dynamic
                          ratio load balancing.
                        type: string
                      monitorMetric:
                        description: The metric of the DTC SNMP monitor used for dynamic
                          weighing.
                        type: string
                      monitorWeighing:
                        description: 'How the DTC monitor weight is applied: PRIORITY
                          forwards clients to the least loaded server; RATIO calculates
                          distribution from dynamic weights.'
                        type: string
                    type: object
                  lbPreferredMethod:
                    description: Preferred load balancing method used to select a
                      server from the pool.
                    type: string
                  lbPreferredTopology:
                    description: Topology ruleset name used when lbPreferredMethod=TOPOLOGY.
                    type: string
                  monitors:
                    description: Health monitors associated with the pool.
                    items:
                      description: DTCPoolMonitor identifies a health monitor associated
                        with a DTCPool by its WAPI `_ref` (mirrors the SDK's polymorphic
                        Monitor interface, simplified to a reference — no monitor
                        sub-type is independently cataloged today).
                      properties:
                        monitor:
                          description: Reference (`_ref`) of the monitor associated
                            with the pool, e.g. "dtc:monitor:http/ZG5z...".
                          type: string
                      type: object
                    type: array
                  name:
                    description: DTC Pool display name. Mutable, but changes are not
                      reference-resolved on the fly by referencing DTCLBDN resources
                      — see the DTCLBDN resource note when it is onboarded.
                    type: string
                  quorum:
                    description: Minimum number of monitors that must report a resource
                      as up for it to be available. Only meaningful when availability=QUORUM.
                    format: int32
                    type: integer
                  ref:
                    description: Server-assigned opaque object reference (WAPI `_ref`).
                      Mirrors the crossplane.io/external-name annotation for observability
                      and uptest import verification.
                    type: string
                  servers:
                    description: DTC Servers that are members of this pool, each with
                      a per-server ratio/weight.
                    items:
                      description: DTCPoolServerLink identifies one DTCServer member
                        of a DTCPool along with its load-balancing weight (mirrors
                        the SDK's DtcServerLink struct).
                      properties:
                        ratio:
                          description: The load-balancing weight of the server within
                            the pool.
                          format: int32
                          type: integer
                        server:
                          description: DTCServer member of the pool, identified by
                            its Crossplane external-name (the DTCServer's WAPI `_ref`).
                          type: string
                        serverRef:
                          description: A Reference to a named object.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        serverSelector:
                          description: A Selector selects an object.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  ttl:
                    description: Time-To-Live for the DTC Pool response, in seconds.
                      Zero indicates the record should not be cached. Must be non-negative
                      (0-2147483647); to inherit the zone/grid default, set useTtl
                      to false rather than passing a negative sentinel value.
                    format: int32
                    type: integer
                  useTtl:
                    description: Use flag for ttl.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
