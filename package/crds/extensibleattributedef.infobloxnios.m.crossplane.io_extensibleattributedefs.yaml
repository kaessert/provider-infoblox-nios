---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: extensibleattributedefs.extensibleattributedef.infobloxnios.m.crossplane.io
spec:
  group: extensibleattributedef.infobloxnios.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - infobloxnios
    kind: ExtensibleAttributeDef
    listKind: ExtensibleAttributeDefList
    plural: extensibleattributedefs
    singular: extensibleattributedef
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ExtensibleAttributeDef is the Namespaced-scoped managed resource
          representing an Infoblox NIOS ExtensibleAttributeDef.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ExtensibleAttributeDefSpec defines the desired state of ExtensibleAttributeDef.
            properties:
              driftDetection:
                description: |-
                  DriftDetection configures which forProvider fields are owned
                  outside Crossplane and how drift in those fields is detected and
                  corrected. Absent configuration means drift detection is enabled
                  with no ignored paths -- today's behaviour.
                properties:
                  ignore:
                    description: Ignore declares fields owned outside Crossplane.
                    items:
                      description: |-
                        IgnoreRule declares fields whose value is owned by something other than
                        Crossplane. Listed fields are seeded from spec on create, then neither
                        compared nor written from spec: the value observed on the external
                        resource is carried forward instead.
                      properties:
                        paths:
                          description: |-
                            Paths to ignore, in Crossplane field path notation rooted at the
                            managed resource spec, for example forProvider.comment. This is the
                            grammar Composition patches use; JSON Pointer form
                            (/forProvider/comment) is also accepted. List indices and wildcards
                            are not supported -- ignore the whole list instead.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  mode:
                    default: enabled
                    description: Mode selects the drift detection behaviour.
                    enum:
                    - enabled
                    - warn
                    - disabled
                    type: string
                type: object
              forProvider:
                description: ForProvider holds the user-supplied parameters for this
                  ExtensibleAttributeDef.
                properties:
                  allowedObjectTypes:
                    description: WAPI object types this attribute is allowed to associate
                      with (empty means all types). Mutable.
                    items:
                      type: string
                    type: array
                  comment:
                    description: Comment for the Extensible Attribute Definition;
                      maximum 256 characters. Mutable.
                    type: string
                  defaultValue:
                    description: Default value used to pre-populate the attribute
                      value in the GUI. For email, URL, and string types, a string
                      with a maximum of 256 characters. For an integer, an integer
                      from -2147483648 through 2147483647. For a date, the number
                      of seconds elapsed since January 1st, 1970 UTC. Mutable.
                    type: string
                  descendantsAction:
                    description: Action taken on descendant objects carrying this
                      attribute when it is Inheritable. Write-only — the WAPI field
                      schema marks it not readable, so it is never mirrored into the
                      observed state; a controller must never request it in _return_fields.
                    type: string
                  flags:
                    description: 'Extensible attribute flags. Possible letters, most-significant
                      first: (A)udited, (C)loud API, Cloud (G)master, (I)nheritable,
                      (L)isted, (M)andatory value, MGM (P)rivate, (R)ead Only, (S)ort
                      enum values, Multiple (V)alues. If there are two or more flags
                      they must be listed in the order shown above (e.g. "CR" is valid,
                      "RC" is not). Mutable.'
                    type: string
                  listValues:
                    description: Enumerated valid values. Applicable if type=ENUM.
                      Mutable.
                    items:
                      description: EADefListValue holds one enumerated value entry
                        for an ExtensibleAttributeDef with type=ENUM.
                      properties:
                        value:
                          description: The enumerated value.
                          type: string
                      required:
                      - value
                      type: object
                    type: array
                  max:
                    description: Maximum allowed value of the extensible attribute.
                      Applicable if type=INTEGER. Immutable — same family as min,
                      rejected by WAPI on update.
                    format: int32
                    type: integer
                    x-kubernetes-validations:
                    - message: max is immutable after creation
                      rule: self == oldSelf
                  min:
                    description: Minimum allowed value of the extensible attribute.
                      Applicable if type=INTEGER. Immutable — WAPI rejects a change
                      with "Minimum value cannot be modified".
                    format: int32
                    type: integer
                    x-kubernetes-validations:
                    - message: min is immutable after creation
                      rule: self == oldSelf
                  name:
                    description: Name of the Extensible Attribute Definition. Mutable
                      — renaming changes the record's _ref.
                    type: string
                  type:
                    description: Data type of the extensible attribute's value. Immutable
                      — WAPI rejects a change with "Type of extensible attribute definition
                      cannot be modified".
                    enum:
                    - STRING
                    - INTEGER
                    - ENUM
                    - DATE
                    - EMAIL
                    - URL
                    type: string
                    x-kubernetes-validations:
                    - message: type is immutable after creation
                      rule: self == oldSelf
                required:
                - name
                - type
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ExtensibleAttributeDefStatus defines the observed state of
              ExtensibleAttributeDef.
            properties:
              atProvider:
                description: AtProvider holds the observed values from the Infoblox
                  NIOS WAPI.
                properties:
                  allowedObjectTypes:
                    description: WAPI object types this attribute is allowed to associate
                      with (empty means all types). Mutable.
                    items:
                      type: string
                    type: array
                  comment:
                    description: Comment for the Extensible Attribute Definition;
                      maximum 256 characters. Mutable.
                    type: string
                  defaultValue:
                    description: Default value used to pre-populate the attribute
                      value in the GUI. For email, URL, and string types, a string
                      with a maximum of 256 characters. For an integer, an integer
                      from -2147483648 through 2147483647. For a date, the number
                      of seconds elapsed since January 1st, 1970 UTC. Mutable.
                    type: string
                  flags:
                    description: 'Extensible attribute flags. Possible letters, most-significant
                      first: (A)udited, (C)loud API, Cloud (G)master, (I)nheritable,
                      (L)isted, (M)andatory value, MGM (P)rivate, (R)ead Only, (S)ort
                      enum values, Multiple (V)alues. If there are two or more flags
                      they must be listed in the order shown above (e.g. "CR" is valid,
                      "RC" is not). Mutable.'
                    type: string
                  id:
                    description: |-
                      Server-assigned external identifier for this resource, set by Observe()
                      from the resource's external name. Used by import testing to confirm
                      the controller re-discovers the resource after a restart.
                    type: string
                  listValues:
                    description: Enumerated valid values. Applicable if type=ENUM.
                      Mutable.
                    items:
                      description: EADefListValue holds one enumerated value entry
                        for an ExtensibleAttributeDef with type=ENUM.
                      properties:
                        value:
                          description: The enumerated value.
                          type: string
                      required:
                      - value
                      type: object
                    type: array
                  max:
                    description: Maximum allowed value of the extensible attribute.
                      Applicable if type=INTEGER. Immutable — same family as min,
                      rejected by WAPI on update.
                    format: int32
                    type: integer
                    x-kubernetes-validations:
                    - message: max is immutable after creation
                      rule: self == oldSelf
                  min:
                    description: Minimum allowed value of the extensible attribute.
                      Applicable if type=INTEGER. Immutable — WAPI rejects a change
                      with "Minimum value cannot be modified".
                    format: int32
                    type: integer
                    x-kubernetes-validations:
                    - message: min is immutable after creation
                      rule: self == oldSelf
                  name:
                    description: Name of the Extensible Attribute Definition. Mutable
                      — renaming changes the record's _ref.
                    type: string
                  namespace:
                    description: Namespace for the Extensible Attribute Definition.
                      Read-only.
                    type: string
                  ref:
                    description: Server-assigned opaque object reference (WAPI `_ref`).
                      Mirrors the crossplane.io/external-name annotation for observability
                      and uptest import verification.
                    type: string
                  type:
                    description: Data type of the extensible attribute's value. Immutable
                      — WAPI rejects a change with "Type of extensible attribute definition
                      cannot be modified".
                    type: string
                    x-kubernetes-validations:
                    - message: type is immutable after creation
                      rule: self == oldSelf
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
