---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: hostrecords.hostrecord.infobloxnios.crossplane.io
spec:
  group: hostrecord.infobloxnios.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - infobloxnios
    kind: HostRecord
    listKind: HostRecordList
    plural: hostrecords
    singular: hostrecord
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HostRecord is the Cluster-scoped managed resource representing
          an Infoblox NIOS HostRecord.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HostRecordSpec defines the desired state of HostRecord.
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                description: ForProvider holds the user-supplied parameters for this
                  HostRecord.
                properties:
                  aliases:
                    description: List of alias FQDNs for the host.
                    items:
                      type: string
                    type: array
                  comment:
                    description: Comment for the record; maximum 256 characters.
                    type: string
                  configureForDns:
                    description: Whether the host has DNS (parent zone) information.
                      When false, no DNS records are created.
                    type: boolean
                  disable:
                    description: Whether the record is disabled.
                    type: boolean
                  extAttrs:
                    additionalProperties:
                      type: string
                    description: 'Extensible attributes (arbitrary key/value metadata
                      defined in Grid Manager). The WAPI wire format wraps each value
                      as {"value": ...}; this map is the simplified string-valued
                      CRD representation (the controller translates to/from the SDK''s
                      EA map[string]interface{} type).'
                    type: object
                    x-kubernetes-validations:
                    - message: the 'Crossplane Internal ID' extensible attribute is
                        reserved for the provider's identity stamp and cannot be set
                        in spec.forProvider.extAttrs
                      rule: self.all(k, k != 'Crossplane Internal ID')
                  filterParams:
                    additionalProperties:
                      type: string
                    description: Extensible attribute key/value filter for EA-based
                      IP allocation via AllocateNextAvailableIp. Mutually exclusive
                      with ipv4Cidr/ipv6Cidr. When set, ipAddressType is required.
                    type: object
                  ipAddressType:
                    description: 'IP address type to allocate when using filterParams:
                      "IPV4", "IPV6", or "Both". Required when filterParams is set;
                      ignored otherwise.'
                    enum:
                    - IPV4
                    - IPV6
                    - Both
                    type: string
                  ipv4Addrs:
                    description: List of IPv4 addresses for the host. At least one
                      entry is required on create.
                    items:
                      description: HostRecordIpv4Addr holds one IPv4 address entry
                        for a HostRecord.
                      properties:
                        configureForDhcp:
                          description: Whether to configure the address for DHCP.
                          type: boolean
                        ipv4Addr:
                          description: IPv4 address of the host (static or CIDR-allocated).
                          type: string
                        mac:
                          description: MAC address for DHCP association.
                          type: string
                        nextServer:
                          description: Next server address for PXE boot.
                          type: string
                      required:
                      - ipv4Addr
                      type: object
                    type: array
                  ipv4Cidr:
                    description: CIDR of the IPv4 network from which to allocate the
                      next available IP address (WAPI func:nextavailableip). Create-time-only
                      — ignored on Update. Mutually exclusive with static ipv4Addrs
                      entries that specify an address.
                    type: string
                  ipv6Addrs:
                    description: List of IPv6 addresses for the host.
                    items:
                      description: HostRecordIpv6Addr holds one IPv6 address entry
                        for a HostRecord.
                      properties:
                        configureForDhcp:
                          description: Whether to configure the address for DHCP.
                          type: boolean
                        duid:
                          description: DHCP unique identifier for IPv6 DHCP association.
                          type: string
                        ipv6Addr:
                          description: IPv6 address of the host.
                          type: string
                      required:
                      - ipv6Addr
                      type: object
                    type: array
                  ipv6Cidr:
                    description: CIDR of the IPv6 network from which to allocate the
                      next available IP address. Create-time-only — ignored on Update.
                      Mutually exclusive with static ipv6Addrs entries that specify
                      an address.
                    type: string
                  name:
                    description: Host name in FQDN format. Renaming changes the record's
                      _ref.
                    type: string
                  networkView:
                    description: 'Network view the host record resides in. Immutable
                      after creation (live-verified: supports=rws, no u — corrects
                      Phase 1 inventory).'
                    type: string
                    x-kubernetes-validations:
                    - message: networkView is immutable after creation
                      rule: self == oldSelf
                  networkViewRef:
                    description: A Reference to a named object.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  networkViewSelector:
                    description: A Selector selects an object.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  ttl:
                    description: Time-to-live in seconds. Zero means the record is
                      not cached. Must be non-negative (0-2147483647); to inherit
                      the zone/grid default, set useTtl to false rather than passing
                      a negative sentinel value.
                    format: int32
                    maximum: 2147483647
                    minimum: 0
                    type: integer
                  useTtl:
                    description: Use flag for ttl — when false the zone/grid default
                      TTL applies.
                    type: boolean
                  view:
                    description: DNS view in which the record resides. Mutable — changing
                      it moves the record between DNS views and changes the _ref.
                    type: string
                required:
                - ipv4Addrs
                - name
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: HostRecordStatus defines the observed state of HostRecord.
            properties:
              atProvider:
                description: AtProvider holds the observed values from the Infoblox
                  NIOS WAPI.
                properties:
                  aliases:
                    description: List of alias FQDNs for the host.
                    items:
                      type: string
                    type: array
                  comment:
                    description: Comment for the record; maximum 256 characters.
                    type: string
                  configureForDns:
                    description: Whether the host has DNS (parent zone) information.
                      When false, no DNS records are created.
                    type: boolean
                  disable:
                    description: Whether the record is disabled.
                    type: boolean
                  dnsAliases:
                    description: Alias FQDNs in punycode format.
                    items:
                      type: string
                    type: array
                  dnsName:
                    description: Host name in punycode format (derived from name).
                    type: string
                  extAttrs:
                    additionalProperties:
                      type: string
                    description: 'Extensible attributes (arbitrary key/value metadata
                      defined in Grid Manager). The WAPI wire format wraps each value
                      as {"value": ...}; this map is the simplified string-valued
                      CRD representation (the controller translates to/from the SDK''s
                      EA map[string]interface{} type).'
                    type: object
                  filterParams:
                    additionalProperties:
                      type: string
                    description: Extensible attribute key/value filter for EA-based
                      IP allocation via AllocateNextAvailableIp. Mutually exclusive
                      with ipv4Cidr/ipv6Cidr. When set, ipAddressType is required.
                    type: object
                  id:
                    description: |-
                      Server-assigned external identifier for this resource, set by Observe()
                      from the resource's external name. Used by import testing to confirm
                      the controller re-discovers the resource after a restart.
                    type: string
                  ipAddressType:
                    description: 'IP address type to allocate when using filterParams:
                      "IPV4", "IPV6", or "Both". Required when filterParams is set;
                      ignored otherwise.'
                    type: string
                  ipv4Addrs:
                    description: List of IPv4 addresses for the host. At least one
                      entry is required on create.
                    items:
                      description: HostRecordIpv4Addr holds one IPv4 address entry
                        for a HostRecord.
                      properties:
                        configureForDhcp:
                          description: Whether to configure the address for DHCP.
                          type: boolean
                        ipv4Addr:
                          description: IPv4 address of the host (static or CIDR-allocated).
                          type: string
                        mac:
                          description: MAC address for DHCP association.
                          type: string
                        nextServer:
                          description: Next server address for PXE boot.
                          type: string
                      required:
                      - ipv4Addr
                      type: object
                    type: array
                  ipv4Cidr:
                    description: CIDR of the IPv4 network from which to allocate the
                      next available IP address (WAPI func:nextavailableip). Create-time-only
                      — ignored on Update. Mutually exclusive with static ipv4Addrs
                      entries that specify an address.
                    type: string
                  ipv6Addrs:
                    description: List of IPv6 addresses for the host.
                    items:
                      description: HostRecordIpv6Addr holds one IPv6 address entry
                        for a HostRecord.
                      properties:
                        configureForDhcp:
                          description: Whether to configure the address for DHCP.
                          type: boolean
                        duid:
                          description: DHCP unique identifier for IPv6 DHCP association.
                          type: string
                        ipv6Addr:
                          description: IPv6 address of the host.
                          type: string
                      required:
                      - ipv6Addr
                      type: object
                    type: array
                  ipv6Cidr:
                    description: CIDR of the IPv6 network from which to allocate the
                      next available IP address. Create-time-only — ignored on Update.
                      Mutually exclusive with static ipv6Addrs entries that specify
                      an address.
                    type: string
                  name:
                    description: Host name in FQDN format. Renaming changes the record's
                      _ref.
                    type: string
                  networkView:
                    description: 'Network view the host record resides in. Immutable
                      after creation (live-verified: supports=rws, no u — corrects
                      Phase 1 inventory).'
                    type: string
                    x-kubernetes-validations:
                    - message: networkView is immutable after creation
                      rule: self == oldSelf
                  ref:
                    description: Server-assigned opaque object reference (WAPI `_ref`).
                      Mirrors the crossplane.io/external-name annotation for observability
                      and uptest import verification.
                    type: string
                  ttl:
                    description: Time-to-live in seconds. Zero means the record is
                      not cached. Must be non-negative (0-2147483647); to inherit
                      the zone/grid default, set useTtl to false rather than passing
                      a negative sentinel value.
                    format: int32
                    type: integer
                  useTtl:
                    description: Use flag for ttl — when false the zone/grid default
                      TTL applies.
                    type: boolean
                  view:
                    description: DNS view in which the record resides. Mutable — changing
                      it moves the record between DNS views and changes the _ref.
                    type: string
                  zone:
                    description: Zone in which the record resides. Derived from name/view
                      by WAPI — not a CreateHostRecord parameter.
                    type: string
                    x-kubernetes-validations:
                    - message: zone is immutable after creation
                      rule: self == oldSelf
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
