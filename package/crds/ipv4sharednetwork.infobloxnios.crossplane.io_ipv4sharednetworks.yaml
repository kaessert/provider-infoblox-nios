---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: ipv4sharednetworks.ipv4sharednetwork.infobloxnios.crossplane.io
spec:
  group: ipv4sharednetwork.infobloxnios.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - infobloxnios
    kind: IPv4SharedNetwork
    listKind: IPv4SharedNetworkList
    plural: ipv4sharednetworks
    singular: ipv4sharednetwork
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IPv4SharedNetwork is the Cluster-scoped managed resource representing
          an Infoblox NIOS IPv4SharedNetwork.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IPv4SharedNetworkSpec defines the desired state of IPv4SharedNetwork.
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                description: ForProvider holds the user-supplied parameters for this
                  IPv4SharedNetwork.
                properties:
                  comment:
                    description: Comment for the shared network.
                    type: string
                  disable:
                    description: Whether the shared network is disabled. When false,
                      the shared network is enabled.
                    type: boolean
                  extAttrs:
                    additionalProperties:
                      type: string
                    description: 'Extensible attributes (arbitrary key/value metadata
                      defined in Grid Manager). The WAPI wire format wraps each value
                      as {"value": ...}; this map is the simplified string-valued
                      CRD representation (the controller translates to/from the SDK''s
                      EA map[string]interface{} type).'
                    type: object
                    x-kubernetes-validations:
                    - message: the 'Crossplane Internal ID' extensible attribute is
                        reserved for the provider's identity stamp and cannot be set
                        in spec.forProvider.extAttrs
                      rule: self.all(k, k != 'Crossplane Internal ID')
                  name:
                    description: Display name of the shared network.
                    type: string
                  networkView:
                    description: Network view the shared network belongs to. Present
                      in both the Create and Update ObjectManager wrapper signatures,
                      but live WAPI _schema probing found supports=rws (no u) for
                      this field — the WAPI schema rejects changing it after creation
                      even though the wrapper accepts the parameter on update calls.
                    type: string
                    x-kubernetes-validations:
                    - message: networkView is immutable after creation
                      rule: self == oldSelf
                  networks:
                    description: CIDRs of the member networks combined into this shared
                      network. Each entry must match an existing Network resource's
                      network (CIDR) field — a cidr-match cross-resource reference,
                      not a _ref or name lookup. Set networksRefs/networksSelector
                      to reference Network objects by name; resolution populates each
                      networks entry with the referenced Network's CIDR.
                    items:
                      type: string
                    type: array
                  networksRefs:
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  networksSelector:
                    description: A Selector selects an object.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  options:
                    description: DHCP options associated with the shared network.
                    items:
                      description: IPv4SharedNetworkDhcpOption is a DHCP option associated
                        with an IPv4SharedNetwork, mirroring the SDK's Dhcpoption
                        struct.
                      properties:
                        name:
                          description: Name of the DHCP option.
                          type: string
                        num:
                          description: Code of the DHCP option.
                          format: int32
                          type: integer
                        useOption:
                          description: Only applies to special options displayed separately
                            from other options with a use flag (e.g. routers, domain-name-servers,
                            dhcp-lease-time).
                          type: boolean
                        value:
                          description: Value of the DHCP option.
                          type: string
                        vendorClass:
                          description: Name of the space this DHCP option is associated
                            with.
                          type: string
                      type: object
                    type: array
                  useOptions:
                    description: Use flag for the options field.
                    type: boolean
                required:
                - name
                - networks
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: IPv4SharedNetworkStatus defines the observed state of IPv4SharedNetwork.
            properties:
              atProvider:
                description: AtProvider holds the observed values from the Infoblox
                  NIOS WAPI.
                properties:
                  authority:
                    description: Authority for the shared network.
                    type: boolean
                  comment:
                    description: Comment for the shared network.
                    type: string
                  ddnsTtl:
                    description: DNS update Time-to-live (TTL) value, in seconds.
                      Zero means the update is not cached.
                    format: int32
                    type: integer
                  dhcpUtilization:
                    description: DHCP utilization percentage across member networks,
                      multiplied by 1000.
                    format: int32
                    type: integer
                  dhcpUtilizationStatus:
                    description: Utilization level descriptor (e.g. "NORMAL", "WARNING").
                    type: string
                  disable:
                    description: Whether the shared network is disabled. When false,
                      the shared network is enabled.
                    type: boolean
                  dynamicHosts:
                    description: Total number of DHCP leases issued for the shared
                      network.
                    format: int32
                    type: integer
                  enableDdns:
                    description: Whether the DHCP server sends DDNS updates for this
                      shared network.
                    type: boolean
                  extAttrs:
                    additionalProperties:
                      type: string
                    description: 'Extensible attributes (arbitrary key/value metadata
                      defined in Grid Manager). The WAPI wire format wraps each value
                      as {"value": ...}; this map is the simplified string-valued
                      CRD representation (the controller translates to/from the SDK''s
                      EA map[string]interface{} type).'
                    type: object
                  id:
                    description: |-
                      Server-assigned external identifier for this resource, set by Observe()
                      from the resource's external name. Used by import testing to confirm
                      the controller re-discovers the resource after a restart.
                    type: string
                  name:
                    description: Display name of the shared network.
                    type: string
                  networkView:
                    description: Network view the shared network belongs to. Present
                      in both the Create and Update ObjectManager wrapper signatures,
                      but live WAPI _schema probing found supports=rws (no u) for
                      this field — the WAPI schema rejects changing it after creation
                      even though the wrapper accepts the parameter on update calls.
                    type: string
                    x-kubernetes-validations:
                    - message: networkView is immutable after creation
                      rule: self == oldSelf
                  networks:
                    description: CIDRs of the member networks combined into this shared
                      network. Each entry must match an existing Network resource's
                      network (CIDR) field — a cidr-match cross-resource reference,
                      not a _ref or name lookup. Set networksRefs/networksSelector
                      to reference Network objects by name; resolution populates each
                      networks entry with the referenced Network's CIDR.
                    items:
                      type: string
                    type: array
                  options:
                    description: DHCP options associated with the shared network.
                    items:
                      description: IPv4SharedNetworkDhcpOption is a DHCP option associated
                        with an IPv4SharedNetwork, mirroring the SDK's Dhcpoption
                        struct.
                      properties:
                        name:
                          description: Name of the DHCP option.
                          type: string
                        num:
                          description: Code of the DHCP option.
                          format: int32
                          type: integer
                        useOption:
                          description: Only applies to special options displayed separately
                            from other options with a use flag (e.g. routers, domain-name-servers,
                            dhcp-lease-time).
                          type: boolean
                        value:
                          description: Value of the DHCP option.
                          type: string
                        vendorClass:
                          description: Name of the space this DHCP option is associated
                            with.
                          type: string
                      type: object
                    type: array
                  ref:
                    description: Server-assigned opaque object reference (WAPI `_ref`).
                      Mirrors the crossplane.io/external-name annotation for observability
                      and uptest import verification.
                    type: string
                  useOptions:
                    description: Use flag for the options field.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
