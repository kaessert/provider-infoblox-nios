---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: rangetemplates.rangetemplate.infobloxnios.m.crossplane.io
spec:
  group: rangetemplate.infobloxnios.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - infobloxnios
    kind: RangeTemplate
    listKind: RangeTemplateList
    plural: rangetemplates
    singular: rangetemplate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RangeTemplate is the Namespaced-scoped managed resource representing
          an Infoblox NIOS RangeTemplate.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RangeTemplateSpec defines the desired state of RangeTemplate.
            properties:
              forProvider:
                description: ForProvider holds the user-supplied parameters for this
                  RangeTemplate.
                properties:
                  cloudApiCompatible:
                    description: Whether this template can be used in cloud-computing
                      deployments.
                    type: boolean
                  comment:
                    description: Comment for the range template.
                    type: string
                  extattrs:
                    additionalProperties:
                      type: string
                    description: 'Extensible attributes (arbitrary key/value metadata
                      defined in Grid Manager). The WAPI wire format wraps each value
                      as {"value": ...}; this map is the simplified string-valued
                      CRD representation (the controller translates to/from the SDK''s
                      EA map[string]interface{} type).'
                    type: object
                  failoverAssociation:
                    description: Failover association name.
                    type: string
                  member:
                    description: Grid member that will serve ranges created from this
                      template.
                    properties:
                      ipv4addr:
                        description: The IPv4 Address of the Grid Member.
                        type: string
                      ipv6addr:
                        description: The IPv6 Address of the Grid Member.
                        type: string
                      name:
                        description: The Grid member name.
                        type: string
                    type: object
                  msServer:
                    description: Microsoft DHCP server name (write-only convenience
                      parameter). The SDK wraps this into a nested Msdhcpserver struct
                      persisted on the response as ms_server — a different shape than
                      this flat field, so it is never echoed back and excluded from
                      the AtProvider mirror.
                    type: string
                  name:
                    description: Name of the range template object.
                    type: string
                  numberOfAddresses:
                    description: Number of addresses the range should contain when
                      instantiated.
                    format: int32
                    type: integer
                  offset:
                    description: Start address offset within the parent network when
                      instantiated.
                    format: int32
                    type: integer
                  options:
                    description: DHCP options to apply to ranges created from this
                      template.
                    items:
                      description: RangeTemplateOption carries one DHCP option to
                        apply to ranges created from a RangeTemplate (mirrors the
                        SDK's Dhcpoption struct).
                      properties:
                        name:
                          description: Name of the DHCP option.
                          type: string
                        num:
                          description: The code of the DHCP option.
                          format: int32
                          type: integer
                        useOption:
                          description: Only applies to special options that are displayed
                            separately from other options and have a use flag (routers,
                            router-templates, domain-name-servers, domain-name, broadcast-address,
                            broadcast-address-offset, dhcp-lease-time, dhcp6.name-servers).
                          type: boolean
                        value:
                          description: Value of the DHCP option.
                          type: string
                        vendorClass:
                          description: The name of the space this DHCP option is associated
                            to.
                          type: string
                      type: object
                    type: array
                  serverAssociationType:
                    description: Type of server that will serve ranges created from
                      this template.
                    type: string
                  useOptions:
                    description: Use flag for options.
                    type: boolean
                required:
                - name
                - numberOfAddresses
                - offset
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: RangeTemplateStatus defines the observed state of RangeTemplate.
            properties:
              atProvider:
                description: AtProvider holds the observed values from the Infoblox
                  NIOS WAPI.
                properties:
                  cloudApiCompatible:
                    description: Whether this template can be used in cloud-computing
                      deployments.
                    type: boolean
                  comment:
                    description: Comment for the range template.
                    type: string
                  extattrs:
                    additionalProperties:
                      type: string
                    description: 'Extensible attributes (arbitrary key/value metadata
                      defined in Grid Manager). The WAPI wire format wraps each value
                      as {"value": ...}; this map is the simplified string-valued
                      CRD representation (the controller translates to/from the SDK''s
                      EA map[string]interface{} type).'
                    type: object
                  failoverAssociation:
                    description: Failover association name.
                    type: string
                  id:
                    description: |-
                      Server-assigned external identifier for this resource, set by Observe()
                      from the resource's external name. Used by import testing to confirm
                      the controller re-discovers the resource after a restart.
                    type: string
                  member:
                    description: Grid member that will serve ranges created from this
                      template.
                    properties:
                      ipv4addr:
                        description: The IPv4 Address of the Grid Member.
                        type: string
                      ipv6addr:
                        description: The IPv6 Address of the Grid Member.
                        type: string
                      name:
                        description: The Grid member name.
                        type: string
                    type: object
                  name:
                    description: Name of the range template object.
                    type: string
                  numberOfAddresses:
                    description: Number of addresses the range should contain when
                      instantiated.
                    format: int32
                    type: integer
                  offset:
                    description: Start address offset within the parent network when
                      instantiated.
                    format: int32
                    type: integer
                  options:
                    description: DHCP options to apply to ranges created from this
                      template.
                    items:
                      description: RangeTemplateOption carries one DHCP option to
                        apply to ranges created from a RangeTemplate (mirrors the
                        SDK's Dhcpoption struct).
                      properties:
                        name:
                          description: Name of the DHCP option.
                          type: string
                        num:
                          description: The code of the DHCP option.
                          format: int32
                          type: integer
                        useOption:
                          description: Only applies to special options that are displayed
                            separately from other options and have a use flag (routers,
                            router-templates, domain-name-servers, domain-name, broadcast-address,
                            broadcast-address-offset, dhcp-lease-time, dhcp6.name-servers).
                          type: boolean
                        value:
                          description: Value of the DHCP option.
                          type: string
                        vendorClass:
                          description: The name of the space this DHCP option is associated
                            to.
                          type: string
                      type: object
                    type: array
                  ref:
                    description: Server-assigned opaque object reference (WAPI `_ref`).
                      Mirrors the crossplane.io/external-name annotation for observability
                      and uptest import verification.
                    type: string
                  serverAssociationType:
                    description: Type of server that will serve ranges created from
                      this template.
                    type: string
                  useOptions:
                    description: Use flag for options.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
