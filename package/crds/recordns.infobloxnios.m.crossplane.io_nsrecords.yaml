---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: nsrecords.recordns.infobloxnios.m.crossplane.io
spec:
  group: recordns.infobloxnios.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - infobloxnios
    kind: NSRecord
    listKind: NSRecordList
    plural: nsrecords
    singular: nsrecord
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NSRecord is the Namespaced-scoped managed resource representing
          an Infoblox NIOS NSRecord.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NSRecordSpec defines the desired state of NSRecord.
            properties:
              forProvider:
                description: ForProvider holds the user-supplied parameters for this
                  NSRecord.
                properties:
                  addresses:
                    description: 'Glue address records for the name server. Live-verified
                      REQUIRED on create (`field for create missing: addresses`) —
                      corrects inventory.md''s "optional" classification.'
                    items:
                      description: NSRecordAddress identifies one glue name server
                        address for an NSRecord (mirrors the SDK's ZoneNameServer
                        struct).
                      properties:
                        address:
                          description: The address of the Zone Name Server.
                          type: string
                        autoCreatePtr:
                          description: Flag to indicate if PTR records need to be
                            auto created.
                          type: boolean
                      type: object
                    type: array
                  msDelegationName:
                    description: MS delegation point name.
                    type: string
                  name:
                    description: Name of the NS record in FQDN format (the delegated
                      zone/subdomain). Live-verified immutable (`supports=rws`, no
                      `u`) — WAPI rejects a PUT that changes this field even though
                      the Go SDK's UpdateNSRecord signature still accepts a name parameter.
                    type: string
                    x-kubernetes-validations:
                    - message: name is immutable after creation
                      rule: self == oldSelf
                  nameserver:
                    description: 'FQDN of the authoritative server for the redirected
                      zone. WAPI allows this field to be updated in place, but the
                      provider marks it immutable anyway: this object''s server-assigned
                      handle is derived from (view, name, nameserver), and it is the
                      only one of those three components WAPI does not already reject
                      an update for. Changing it in place would rotate the handle
                      out from under the provider with no way to re-establish which
                      live object the resource still refers to, so an apply that changes
                      it is rejected by the API server and the existing record is
                      left unchanged. An operator who needs a different name server
                      replaces the resource deliberately.'
                    type: string
                    x-kubernetes-validations:
                    - message: nameserver is immutable after creation
                      rule: self == oldSelf
                  view:
                    description: DNS view in which the record resides, e.g. "external".
                      Fixed at creation — WAPI ties the record's _ref to (view, zone,
                      name). Live-verified hard immutable (`supports=rws`, no `u`);
                      the Go SDK already drops view from UpdateNSRecord's request
                      body.
                    type: string
                    x-kubernetes-validations:
                    - message: view is immutable after creation
                      rule: self == oldSelf
                required:
                - addresses
                - name
                - nameserver
                - view
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: NSRecordStatus defines the observed state of NSRecord.
            properties:
              atProvider:
                description: AtProvider holds the observed values from the Infoblox
                  NIOS WAPI.
                properties:
                  addresses:
                    description: 'Glue address records for the name server. Live-verified
                      REQUIRED on create (`field for create missing: addresses`) —
                      corrects inventory.md''s "optional" classification.'
                    items:
                      description: NSRecordAddress identifies one glue name server
                        address for an NSRecord (mirrors the SDK's ZoneNameServer
                        struct).
                      properties:
                        address:
                          description: The address of the Zone Name Server.
                          type: string
                        autoCreatePtr:
                          description: Flag to indicate if PTR records need to be
                            auto created.
                          type: boolean
                      type: object
                    type: array
                  cloudInfo:
                    description: Cloud API related information for this object (cloud-managed
                      records only).
                    properties:
                      authorityType:
                        description: Type of authority over the object.
                        type: string
                      delegatedMember:
                        description: The Cloud Platform Appliance to which authority
                          of the object is delegated.
                        properties:
                          ipv4addr:
                            description: The IPv4 Address of the Grid Member.
                            type: string
                          ipv6addr:
                            description: The IPv6 Address of the Grid Member.
                            type: string
                          name:
                            description: The Grid member name
                            type: string
                        type: object
                      delegatedRoot:
                        description: Indicates the root of the delegation if delegated_scope
                          is SUBTREE or RECLAIMING. This is not set otherwise.
                        type: string
                      delegatedScope:
                        description: 'Indicates the scope of delegation for the object.
                          This can be one of the following: NONE (outside any delegation),
                          ROOT (the delegation point), SUBTREE (within the scope of
                          a delegation), RECLAIMING (within the scope of a delegation
                          being reclaimed, either as the delegation point or in the
                          subtree).'
                        type: string
                      mgmtPlatform:
                        description: Indicates the specified cloud management platform.
                        type: string
                      ownedByAdaptor:
                        description: Determines whether the object was created by
                          the cloud adapter or not.
                        type: boolean
                      tenant:
                        description: Reference to the tenant object associated with
                          the object, if any.
                        type: string
                      usage:
                        description: Indicates the cloud origin of the object.
                        type: string
                    type: object
                  creator:
                    description: Record creator. Live-verified read-only (`supports=rs`)
                      — present on the RecordNS struct but omitted from inventory.md's
                      static field table.
                    type: string
                  dnsName:
                    description: Record name in punycode format (derived from name).
                    type: string
                  id:
                    description: |-
                      Server-assigned external identifier for this resource, set by Observe()
                      from the resource's external name. Used by import testing to confirm
                      the controller re-discovers the resource after a restart.
                    type: string
                  lastQueried:
                    description: Time of the last DNS query for this record (Unix
                      epoch seconds).
                    format: int64
                    type: integer
                  msDelegationName:
                    description: MS delegation point name.
                    type: string
                  name:
                    description: Name of the NS record in FQDN format (the delegated
                      zone/subdomain). Live-verified immutable (`supports=rws`, no
                      `u`) — WAPI rejects a PUT that changes this field even though
                      the Go SDK's UpdateNSRecord signature still accepts a name parameter.
                    type: string
                    x-kubernetes-validations:
                    - message: name is immutable after creation
                      rule: self == oldSelf
                  nameserver:
                    description: 'FQDN of the authoritative server for the redirected
                      zone. WAPI allows this field to be updated in place, but the
                      provider marks it immutable anyway: this object''s server-assigned
                      handle is derived from (view, name, nameserver), and it is the
                      only one of those three components WAPI does not already reject
                      an update for. Changing it in place would rotate the handle
                      out from under the provider with no way to re-establish which
                      live object the resource still refers to, so an apply that changes
                      it is rejected by the API server and the existing record is
                      left unchanged. An operator who needs a different name server
                      replaces the resource deliberately.'
                    type: string
                    x-kubernetes-validations:
                    - message: nameserver is immutable after creation
                      rule: self == oldSelf
                  policy:
                    description: Host name policy for the record.
                    type: string
                  ref:
                    description: Server-assigned opaque object reference (WAPI `_ref`).
                      Mirrors the crossplane.io/external-name annotation for observability
                      and uptest import verification.
                    type: string
                  view:
                    description: DNS view in which the record resides, e.g. "external".
                      Fixed at creation — WAPI ties the record's _ref to (view, zone,
                      name). Live-verified hard immutable (`supports=rws`, no `u`);
                      the Go SDK already drops view from UpdateNSRecord's request
                      body.
                    type: string
                    x-kubernetes-validations:
                    - message: view is immutable after creation
                      rule: self == oldSelf
                  zone:
                    description: Zone in which the record resides, e.g. "zone.com".
                      Derived from name/view by WAPI — not a CreateNSRecord parameter,
                      so it has no ForProvider counterpart.
                    type: string
                    x-kubernetes-validations:
                    - message: zone is immutable after creation
                      rule: self == oldSelf
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
